# vision: create-surrogate (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T18:01:24  DECISION: ISAAC'S DESIGN EXTENSION (2026-06-10, near-verbatim): 'its like dovetailed — we join these two representations of the same SM. So we can just make a function that makes a CompoctopusAgent or a KBSM or whatever style that is, and also a Carton SM... that would be create_surrogate() to be honest. Then it would bake the agent with the identity and everything and just compile it with the SM so it knows its core, and it self-mods the SM during night by queueing up what it will be in the next time (if it changes). So youd have this create_surrogate() on carton and then you could call it to make these domain constrained agents... its sort of similar to carton identity already and persona — i think its part of that system. And then i think the real point here is to make them triggerable from conditions in the graph properties. This is probably where SOMA comes back in — or something pseudo-SOMA that is sort of filling its place until we have it really ready, and is just firing the trigger condition checks and stuff.' STRUCTURE: (1) DOVETAIL: agent-side SM (KBSM/compoctopus) + graph-side SM (carton flow layer) = TWO REPRESENTATIONS OF ONE SM, compiled together by one call — exactly the 06-04 'same machine, two substrates' line, now made a constructor. (2) create_surrogate() on carton: mints identity + equips graph SM + compiles agent SM 'so it knows its core' = domain-constrained agent; folds into the EXISTING carton identity/persona system (observe_from_identity_pov + Identity_Collection + equip_persona/skillmanager). (3) NIGHT SELF-MOD WITH QUEUED NEXT-SELF: the surrogate does not mutate itself mid-lifetime; during night it QUEUES what it will be next cycle — staged identity evolution (the toy's evolution phase, but agent-proposed instead of random mutation). (4) THE REAL POINT: surrogates TRIGGERABLE FROM CONDITIONS IN GRAPH PROPERTIES — generalizes the toy's trigger_traversal (fires on query-result contact) to STANDING property-conditions; the condition-checker = pseudo-SOMA (a simple property-condition evaluator daemon filling SOMA's place until SOMA :8091 is really ready; SOMA later = the real reasoner doing the trigger checks). EXISTING SUBSTRATE per piece: identity/persona (carton has it), agent SM (KBSM canonical heaven + compoctopus), graph SM (step-3 flow layer = the port), firing loop (cave heartbeat/cron automations or a daemon thread like observation_worker's linker_thread). NEW BUILDS: the dovetail compiler create_surrogate(), the queued-next-self mechanism, the pseudo-SOMA condition daemon. SEQUENCING: step-3 flow layer is the SUBSTRATE create_surrogate rides on; surrogates + pseudo-SOMA extend the chain after it.  tags:[create-surrogate, dovetail, pseudo-soma, identity, persona]
- [v2]  2026-06-23T04:36:51  DECISION: DESIGN-OF-RECORD (the 06-20 audit explicitly wanted this written; Isaac 2026-06-23 'keep going, make it make sense'). THE ACTUAL IDENTITY MODEL = the Ghost/Shell/Gear/cursor persona decomposition (decided 2026-06-20T13:28): an identity is an Agent_Identity ENTITY = a persona's durable core = GHOST (system-prompt / role-frame / CoR) + SHELL (Identity_Collection, the knowledge/retrieval map) + GEAR (skillset + mcp_set + observation-frames) + CURSOR (Execution_State from sm_gate). The persona's carton_identity handle points to the entity; equip_persona populates it; the SM gate's actor IS the entity (Track A DONE: resolve_identity_entity + HAS_COLLECTION + Execution_State on the entity = the cursor+shell legs already wired). AUDIT FACTS (06-17): observation/identity/collection WORKS=KEEP; equip_frame=ORPHAN (carton_frames.json 10 lenses, persists nothing, no caller); the Persona model {frame,skillset,mcp_set,carton_identity} only wired skillset; frames 'were supposed to BE personas but diverged' (jump-flows never built). CHANGE-SET (06-20T14:01): (1) skill->SM [done] (2) graph->system-prompt [exists: sm_gate.generate_system_prompt] (3) equip_persona wire set AGENT_IDENTITY=carton_identity [partly: set_active_identity called by sancrev persona_manager] (4) d-chains on vaulted SM types. THE BUILD (SOT, coarse->fine, replace-before-remove): S1 VAULT Agent_Identity as a typed thing (gnosys-vault Pydantic model frame+collection REQUIRED / skillset+execution_state OPTIONAL + register()+d-chains; isolated :8093 test; fixes the mereo_error 'Agent_Identity not a known type'); S2 entity carries its parts (Ghost-frame + Gear-skillset as graph parts, HAS_COLLECTION done); S3 wire the LIVE sancrev persona_manager.activate_persona to ensure the entity; S4 collection->entity transition (add resolve_identity_entity to _ensure_identity_collection_exists, non-breaking); S5 clean the stubs (GNO_SYS_Identity/Gnosys_Identity_State/Relational_Identity_Pattern). OUT OF SCOPE / Isaac-owned: the GHOST system-prompt SWAP via claude -p (the 'ONE hard host-side'); base/heaven-tree-repl + base/heaven-framework (off-limits); the equip_frame observation-lens wiring (unless trivial). KEY COHERENCE RISK (verify in S1 via the isolated daemon): the Title-vs-snake type-name match — my resolve_identity_entity MERGEs IS_A 'Agent_Identity' (Title); vault normalizes to 'agent_identity' (snake); the entity's IS_A target must be the SAME node SOMA recognizes as the vaulted type, or it stays mereo_error. Live equip_persona=sancrev persona_manager.py (editable); heaven-tree-repl/heaven-framework OFF-LIMITS.  tags:[Agent_Identity, Persona, Frames, Vault, Design_Of_Record]
