# vision: meta-operations (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T12:52:44  FINDING: MAJOR CORRECTION + RETRACTION (read meta_operations.py all 1268 lines myself). RUNTIME TREESHELL NODE PROGRAMMING IS FULLY IMPLEMENTED AND WORKING — Isaac was right, my 'never fully implemented' statement was WRONG and is RETRACTED. _meta_add_node (meta_operations.py:202-296): say a node dict {type:Callable, prompt, function_name, is_async, import_path, import_object, args_schema} from inside treeshell -> validates callable fields :266-273 -> _process_callable_node :276 -> inserts LIVE into running tree :283 -> persists :286 via _persist_node_to_heaven_data_dir :400-470 which writes the COMPLETE callable block (function_name/is_async/args_schema/import_path/import_object :439-450) to {app_id}_{version}/configs/families/{family}_family.json in HEAVEN_DATA_DIR. Live dirs confirmed on box: gnosys_treeshell_v1_0, base_treeshell_v1_1, skillmanager_treeshell_v1_0 (earlier miss: I searched only sancrev_treeshell* dirname). _meta_update_node :298-324 = live edit, same persistence. PRECISE PICTURE: TWO parallel node-programming surfaces that do not share storage: SURFACE A (meta_operations -> heaven_data family JSON) = full/executable/hot/persistent, COMPLETE; SURFACE B (carton concept -> JIT) = browse-only menus (JIT never copies function fields). UNIFICATION NOW TRIVIALLY DEFINED: _meta_add_node's node_data dict IS the contract — unify = JIT builds THAT SAME DICT from a concept (function fields as properties or in the already-synced NODE_DEF) instead of only family JSONs. LABEL RETIRED: 'D2' = ONLY the description-rollup mechanism (the label in add_concept_tool.py:1897 docstring + :2151 'D1' comment + 'D21' ref — numbered design items from a prior session, source doc unknown); my coined 'D2-pattern ghost' generalization is RETIRED; plain statements instead: rollup fn = exists-zero-callers; ingestion-dead note = stale; add_carton_node/sync_with_carton = recorded in 2025 observation, no implementation on disk, while the EQUIVALENT capability (runtime callable-node creation) EXISTS as meta_operations = what Isaac remembered. STANDING RULE (Isaac, this session): investigation agents read ONLY the monorepo + treeshell node configs in heaven_data — nothing else, no dev clones, no /core, no site-packages.  tags:[meta-operations, retraction, two-surfaces, agent-scope-rule]
