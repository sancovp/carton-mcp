# vision: Step3_Fence_Opacity_Verified_E2E (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-09T21:57:48  FINDING: STEP 3 FENCE-OPACITY VERIFIED E2E through the real daemon write path (commander, raw n.d from neo4j). ROOT CAUSE confirmed in code: _auto_link_core (add_concept_tool.py) line 467 re.sub(r'[\[\]]','') strips ALL square brackets in its de-nesting pre-pass -> deleted JSON array brackets (the ONLY unrecoverable corruption; token-linkification is recoverable + per-spec refs stay raw in storage, project to links at read/publish time). FIX: auto_link_description is now a wrapper that MASKS each full <CartonObj> span with a Private-Use sentinel (chr 0xE000+i) before _auto_link_core, restores VERBATIM after; hooked at the one chokepoint so it covers all callers incl linker_thread. E2E PROOF (Carton_Kv_Gate2 created via add_concept, daemon PID 12012 new code linkified it): reg fence byte-intact — arrays ['clone','install'] + bare-ref array [A_Unit,B_Unit] brackets PRESERVED, bare ref Cave_Repo stays BARE, ZERO _itself.md inside fence, open tag <CartonObj name=reg> un-linkified; second fence [10,20] preserved; AND prose OUTSIDE the fence linked normally (Publishing_Registry/Doc_Mirror/Carton_Kv_System/etc) = linking still works, only fences masked. Lib tests under my hand: 4/4 fence-opacity + 42/42 + 8/8 regression. Isaac's reframe was right: linking-everything is the DESIGN, not the bug; the bug was the one bracket-strip line, and fence-masking around it is the whole fix. Now running the edit_carton_obj round-trip (2nd half of the combined gate).  tags:[Step3_Fence_Opacity_Verified_E2E, Linker_Root_Cause]
