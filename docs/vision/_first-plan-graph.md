# vision: carton-power-chain (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T16:51:33  DECISION: THE CHAIN DECIDED + INSTANTIATED AS THE FIRST PROPERTY-TRACKED PLAN-GRAPH (the Phase-1 dogfood is now LIVE on real work, not a toy). Carton_Power_Chain {status:active, current_order:1} HAS_STEP 5 steps with {status, order, size} properties: (1) Close_Phase1 [open,small] = re-derive doc(m) server_fastmcp+carton_utils + commit; (2) Small_Fixes [open,small] = render_through_template properties-merge + chroma cache one-liner :2675 + edit-carton-kv skill dead-path correction (one builder batch); (3) Retrieval_As_Data [open,medium] = Retrieval_Protocol/Step nodes + run_retrieval runner, DMN step-0 as first stored step; (4) Publishing_Powered [open,large] = registry re-home as property-nodes + publish_manifest template + hydrator (manifest = RENDER of graph) + PV2-0 resume + PV2 ladder as status-items; (5) Deferred_Tier [deferred,large] = treeshell strangler + personas/frames + SOMA. PROOF RUN: query_by_properties(status=open) returns exactly the 4 open steps in ONE call = the rehydrate-work-state-in-one-query acceptance criterion, met on day one. DISCIPLINE: statuses flipped via set_properties as steps land; never a duplicate concept; next sessions rehydrate the chain via the one query.  tags:[carton-power-chain, first-plan-graph, dogfood-started]
