# vision: pv2-0 (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T03:18:42  DECISION: PV2-0 investigation VERIFIED myself (3 load-bearing claims confirmed in code): (1) codenose CA overlay is IN-FILE not a separable dir (codenose_posttool.py: CA_MCP_DIR:22 + _queue_ca_refresh:25-44 + _try_flush_stale_queues:47-65 + call:82-83) -> path-exclude can't strip it; (2) publish-subdir.yml is TEMPLATE-ONLY (scalable-publishing/templates/), NOT installed in monorepo .github/workflows -> the tag automation was never armed; (3) publish_unit.sh rsync excludes are GLOBAL target-managed files (:71-75), no per-unit overlay mechanism. DESIGN DECISION (commander, avoids dual-file drift that every-build-ends-in-a-dev-flow guards against): the overlay mechanism = a GENERAL per-unit MARKED-REGION strip — wrap overlay lines in '# >>> GNOSYS-CA-OVERLAY' / '# <<< GNOSYS-CA-OVERLAY' in the SINGLE real file, declare overlay-strip files per-unit in publish-manifest.json, publish_unit.sh strips marked regions from the stage before rsync. One file (no drift), reusable for the 'hundreds' of future units. SCOPE NOW: build + LOCAL E2E prove (project codenose to a temp bare repo, assert 0 CA refs in the projected hook, base hook still imports/runs). GATED to Isaac (outward/irreversible, NOT now): actual public push to sancovp/codenose, arming the CI workflow on the default branch, PUBLISH_TOKEN, superseding /tmp/codenose d3e0b00.  tags:[pv2-0, codenose, overlay-strip, design]
