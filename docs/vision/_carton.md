# vision: carton (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T03:40:37  HYPOTHESIS: ISAAC HYPOTHESIS (deconfabulate loop trigger): CartON originally (markdown / github-mirror era, concepts as {Concept}_itself.md files with wiki-links) ROLLED UP a concept's RELATIONSHIPS INTO its DESCRIPTION — the graph structure composed the description = an organizational principle. When CartON moved to neo4j, that rollup may have been UNDONE and replaced with a plain string n.d + separate edges, so the organizational principle was NEVER used on neo4j. IF TRUE: the blob-preference / can't-edit-description / duplicate-concepts failure (this whole conversation) is a REGRESSION from a lost rollup mechanism, not an inherent flaw — and CartON-KV fences are us re-inventing, in a text blob, the description-composition the system used to do from the graph. Connects to: the fence-as-graph-view synthesis; why descriptions became un-editable blobs. DISPATCHED an archaeology subagent to trace the carton codebase history (markdown-rollup era -> neo4j-migration decoherence point), pull old versions to /tmp, ground in commits/code. Per Isaac. UNVERIFIED until the archaeology lands + I re-check the load-bearing commits myself.  tags:[carton, description-rollup, neo4j-migration, deconfabulate, regression]
- [v2]  2026-06-10T03:45:13  FINDING: HYPOTHESIS CONFIRMED in code (I read the legacy file myself, Isaac authorized /core/ for this). LEGACY markdown/github-mirror-era add_concept_tool_func (/home/GOD/core/computer_use_demo/tools/base/tools/add_concept_tool.py:94-172) ROLLED UP relationships into a composed concept document: (1) main {Concept}.md = '# Title' + '## Overview'(description) + '## Relationships' with a '### {rel_type} Relationships' section per relationship listing items (:152-166); (2) per-relationship-type component files components/{rel_type}/{Concept}_{rel_type}.md with each related concept as a clickable GITHUB LINK '[{item}]({url})' (:131-145) = navigable rollup; (3) a concept = a structured DIRECTORY concepts/{Concept}/components/{rel_type}/ (:118-122). ORGANIZING PRINCIPLE = a concept's rendered form was prose-Overview + relationships-rolled-up-as-linked-sections; the description was only the Overview SLICE of a graph-composed document, never freeform-everything. DECOHERENCE = neo4j migration kept ONLY the bare Overview string as n.d; relationships -> separate edges; get_concept appends a FLAT Rels:[...] at read, NOT the structured rolled-up linked document. The composition-as-organizing-principle was never ported to neo4j. (CONFIRMED: legacy rollup, read directly. NOT-yet-pinned: the exact migration commit that dropped it. Current write path stores n.d = raw description = composition gone.) IMPLICATION: the blob-preference / un-editable-description / duplicate-concepts failure is a REGRESSION from this lost rollup; CartON-KV fences = re-inventing by hand the graph-composition carton used to do natively. NEXT: pin the migration commit + decide whether to RESTORE rollup-composition (get_concept composes description FROM relationships, navigable) as the real fix vs the fence workaround.  tags:[carton, rollup-confirmed, decoherence, deconfabulate]
- [v3]  2026-06-21T03:45:19  INTENT: Adding a TYPED-ROUTER to sync_db_graphs_to_carton: a parsed inline-🐉🦴 graph whose is_a==State_Machine routes to carton_mcp.sm_gate.create_sm_chain_live (the SM FACTORY) instead of the untyped queue path; non-SM graphs keep exact queue behavior. New _sm_graph_to_factory builds the 2-SM gating stack (show-SM + gating SM from Traversal_Step nested children) and never raises.  tags:[node_sync, sm_gate, state_machine, carton]
- [v4]  2026-06-21T03:48:16  FINDING: TYPED-ROUTER landed + verified: parser self-tests 32/0 (untouched-green), tests/test_sm_typed_router.py ALL PASS x2 deterministic (live neo4j). SM graph (is_a==State_Machine) routes to create_sm_chain_live building a 2-SM stack -> Concept HAS_SM_CHAIN with 2 SM_CHAIN_RUNS, gating step required_pattern=query_wiki_graph, sm_chain_visit ARMS require_next. Non-SM graph (is_a Skill) still writes a queue file (regression unbroken). pip install --no-deps done. KEY: the test ONLY surfaces the router correctly AFTER pip install (the script run resolves the installed heaven_tree_repl) — early FAIL was the stale installed copy, not the code.  tags:[node_sync, sm_gate, state_machine, carton, test]
- [v5]  2026-06-23T03:48:05  FINDING: FIXED test_sm_core_live_wiring.py (matrix failure #2, was 3/4). TWO root causes, traced to exact symptoms (no masking): (1) STALE SEED vs STACK-SIZE ACTIVATION (sm_gate.py:399, Isaac 2026-06-20: Sm_Chain ON iff stack >1 SM; lone SM=OFF). _program() seeded a 1-SM Core -> sm_chain_visit returned require_next=None -> case2 never armed -> 3/4 cascade. FIX: seed a 2-SM gating stack via create_sm_chain (order-0 show-SM + order-1 gating SM requiring query_wiki_graph; sm_chain_name=Zztest_Lw_Core). (2) ACTOR-IDENTITY OVERRIDE: srv._sm_actor() resolves get_active_identity()='gnosys' FIRST, overriding CARTON_SM_ACTOR -> live gate locked 'gnosys' not the test actor, and _lock_into_sm_chain MATCHes (not MERGEs) the actor node so the lock silently no-op'd; case4 read the wrong actor. FIX: pin srv._sm_actor=lambda:'Zztest_Lw_Actor' (hermetic; still exercises real gate_call/sm_chain_visit/_lock_into_sm_chain/get_active_step). 4/4 PASS live. get_concept WIRING was CORRECT (server_fastmcp.py:1569 _sm_gate_check + :1729 _sm_chain_require_next; _sm_gate_check RE-RAISES GateRefusal :188) -- failures were test-setup, same staleness class as test_sm_ec_e2e. LATENT FINDING (UNVERIFIED, reconnect-gated, NOT fixed -- Isaac's call): active identity 'gnosys' has ZERO :Wiki nodes (count=0 verified), so with CARTON_SM_GATE_ENABLED on in prod, _lock_into_sm_chain MATCH(a:Wiki{n:'gnosys'}) no-ops -> sm_chain_visit STILL appends the require-next note (line 419 unchecked) but NEVER locks -> routing-persistence armed-in-name but NOT enforced for any actor lacking a :Wiki node. Fix at reconnect: give the active identity a :Wiki node OR MERGE the actor in _lock_into_sm_chain (risk: junk nodes). CARTON_SM_GATE is default-OFF so current carton behavior is unaffected.  tags:[Sm_Gate, Get_Concept, Stack_Size_Activation, Carton]
- [v6]  2026-07-04T04:01:09  FINDING: FINDING: Post-compact-12 boot (2026-07-04). Rehydrated per DMN: conversation-0 = 5 entries read in full (2026-07-04T02:28:52 through 03:37:21, the post-compact-11 boot through the personal_domain fix, the enforcement-bypass audit, and the verify-rehydration-before-compact close-out). All four touched cohered notes read in full, consistent with the timeline, zero new contradictions. Cursor confirmed idle, pathway matches the journal (all remaining backlog gated on Isaac). Found the tree was NOT clean at boot -- the webbing-agent coherence daemon (task 51: webbing_agent.py, webbing_agent_worker.py, test_webbing_agent.py, the dev-flow skill/rule, four auto-generated vision-doc stubs, the SYSTEM_SOURCES addition in observation_worker_daemon.py, and this conversations own journal deltas) had been fully built and E2E-verified last session but never committed. This is not a new decision needing Isaacs go-ahead -- the open judgment call he flagged was specifically the live-flag flip and eligibility thresholds, not whether to persist already-verified dry-shipping code -- so committed it directly, per the standing discipline against leaving verified work uncommitted across a compact. Also hit the same recurring gap as be62089 and the post-compact-10 entry: the blanket .gitignore:32 .claude/ rule was silently excluding knowledge/carton-mcp/.claude/ (the new dev-flow skill/rule plus three pre-existing untracked rule files plus CLAUDE.md) -- force-added the whole directory per the established carry-forward pattern. Committed as 4565dc5, pushed, HEAD==origin/main, tree clean. NEXT: nothing newly unblocked -- everything remains gated on Isaac (task 58 enforcement-gap fix, SOMA mereo-C, task 52 CI review, task 47, task 57 query-cache, webbing-agent live-flag flip). Going idle.  tags:[carton, webbing-agent, git-durability]
- [v7]  2026-07-04T04:18:55  FINDING: FINDING: Task 58 done, per Isaacs direct scoping (2026-07-04, verbatim): it can move into add concept tool func but as optionals because it cant change anything about how other code uses carton as lib unless we wanna go thru literally all the code and check for these calls and adjust them. add_concept_tool_func gained four new OPTIONAL params (domain, subdomain, personal_domain, produces), append-only after every existing param since callers use it positionally, so all ~8 existing bypass callers (Dragonbones compiler.py, sm_gate.py, split_content_concept, migration scripts) remain completely unaffected. Read add_concept_tool.py in full around the relevant region and server_fastmcp.pys add_concept MCP tool (lines 470-593) to mirror its has_domain/has_subdomain/has_personal_domain/produces convenience-building exactly, minus the requirement. New pure helper merge_optional_domain_fields, unit tested (7 green, test_concept_provenance_optional_fields.py). Caught a real bug myself during E2E verification, not just trusting green unit tests: the first draft merged the new fields into relationship_dict only -- that validates correctly via SOMA and D2 (a plausible-looking D2 coverage message even appears, listing the fields as declared-but-unmatched) but relationship_dict is a DERIVED view used only for SOMA/D2 validation -- the actual daemon queue write persists the ORIGINAL relationships list verbatim (queue_data relationships = relationships), so the fields never reached the graph at all. Only re-querying the live graph after the call, not the green tests or the plausible D2 message, surfaced this. Fixed by operating on the relationships list itself and reassigning it before relationship_dict is built. Verified live twice after the fix: a direct add_concept_tool_func call with all four fields lands real HAS_DOMAIN/HAS_SUBDOMAIN/HAS_PERSONAL_DOMAIN/PRODUCES edges (personal_domain normalized to Cave title-case per the naming convention); a zero-kwarg old-style call, matching every existing bypass caller exactly, still succeeds identically to before this change. Re-ran test_description_rollup/test_d2_coverage/test_split_content/test_relationship_constraints -- all green, no regressions. Wrote the dev-flow skill (edit-add-concept-optional-fields) plus repo rule per every-build-ends-in-a-development-flow-skill, explicitly documenting the list-vs-dict bug so it cannot be silently reintroduced. Committed+pushed as f0b5e60 (also force-added the same recurring .claude gitignore gap a third time this session). NEXT: genuinely still open, unchanged -- this does NOT migrate any of the ~8 bypass call sites to actually pass the new fields; that audit-and-fix pass is separate future work, not yet scoped or actioned, per Isaacs own framing that going through all the calling code is out of scope right now.  tags:[carton, add-concept, task-58]
- [v8]  2026-07-04T04:48:58  FINDING: FINDING: Task 58 follow-through -- sm_gate.py and dragonbones now REQUIRE domain/subdomain/personal_domain for the SM abstraction, per Isaacs direct reconsideration: is that work actually out of scope? ... Dragonbones should definitely require it, sm_gate should require it for the abstraction about the sm... see? then also necessitates changes to dragonbones again. Unlike add_concept_tool_func (kept optional, ~8 existing bypass callers across the monorepo), create_sm_chain/create_sm_chain_live are this sessions own new capability with exactly 4 known callers, all updated together, so requiring them carries none of that risk. Before touching code, pulled Pattern_Dragonbones_Is_Only_Syntax_Layer (a 2026-05-12 decision that Dragonbones must never be a shape/type registry in Python) and reconciled it: that pattern targets ontological TYPE-SHAPE dicts (GIINT_EC_SHAPES etc), a different category from administrative provenance-field requiring, which already has precedent at the MCP-tool layer (add_concept requires these same 4 fields, accepted without objection) -- proceeded on that basis rather than asking, per the recent-conversation-first deconfabulation discipline. Read compiler.py, logs.py, db_parser_v2.py, db_carton.py, node_sync.py, substrate_projector.py, and server_fastmcp.py in full before editing. Key finding: the EC notation ALREADY supports arbitrary key-EQ-value customs (has_domain/has_subdomain/has_personal_domain need zero parser changes, authored exactly like the existing gates custom). Implemented: sm_gate.create_sm_chain/create_sm_chain_live now raise if domain/subdomain/personal_domain missing or personal_domain fails PERSONAL_DOMAINS validation; tags the Sm_Chain node (not every State_Machine/Traversal_Step, which would be redundant) with HAS_DOMAIN/HAS_SUBDOMAIN/HAS_PERSONAL_DOMAIN plus PRODUCES if given. Updated all 4 production call sites (substrate_projector.project_state_machine reads the SM concepts own has_domain/has_subdomain/has_personal_domain and passes through, refusing cleanly with a specific message if missing; db_carton.py and node_sync.py _sm_graph_to_factory, both copies in lockstep, read the same fields off the parsed graphs customs dict; server_fastmcp.create_branching_sm gained the same required params) plus 10 test files (7 direct call-site updates across 6 files, 3 files that manually seed neo4j for project_state_machine or the typed router). Caught and fixed a REAL bug during E2E verification, not just green unit tests: project_state_machine reads personal_domain back off the graph as a Title_Cased node name (Cave, since the daemon Title-Cases every relationship target it writes), but create_sm_chain enum-validated case-sensitively against the lowercase PERSONAL_DOMAINS list, so this exact round-trip raised -- caught by test_project_state_machine_branching_e2e.py, which I ran live, not assumed passing. Fixed by validating case-insensitively (both the raw lowercase form and the Title_Cased round-trip form are legitimate) and Title-Casing for the write regardless. Re-verified green after the fix: test_sm_branching.py 19/19 unit, plus the full live E2E regression set across carton-mcp, dragonbones, and heaven-tree-repl, all green. Also ran a fresh direct create_sm_chain_live call confirming missing-fields raises, invalid personal_domain raises naming the valid enum, and a full valid call lands all four relationship types on the Sm_Chain (verified via query_wiki_graph, not just a success return). Updated the dragonbones-carton-retrieval-state-machines dev-flow skill with a full Step 5 section matching its established per-step style, plus updated all three authoring-path examples (dragonbones EC, treeshell inline, create_branching_sm) to show the new required fields. Committed+pushed as a9555c0 across all three repos in one commit (monorepo). NEXT: explicitly flagged, NOT actioned -- the GENERAL requirement across every Dragonbones-compiled concept (not just State_Machines), i.e. compiler.pys two general add_concept call sites plus logs.pys CogLog/SkillLog/DeliverableLog, is a much larger system-wide change (Dragonbones is the unified output format for every turn) with a fundamentally different risk profile than this SM-specific change. Recommending Isaac decide explicitly whether/how to scope that before it gets forced through, rather than doing it silently in this same pass.  tags:[carton, sm-gate, dragonbones, task-58]
- [v9]  2026-07-04T05:14:54  FINDING: FINDING: Isaac asked to burn 20pct of weekly quota in 4 hours via multiple concurrent Opus agents webbing the past ~4 weeks journal backlog into CartON (turning unstructured journal prose into real add_concept relationships, converting VISIONs into Task nodes). Launched 4 background Opus agents (Webber_Week0 through Week3, one per calendar week: 06-06to06-11, 06-13to06-19, 06-20to06-26, 06-27to07-03), each with its own Claude Code task (1-4) and coordinating via SendMessage. IMPORTANT SELF-CORRECTION SAGA, recorded honestly: Isaac said the Sophia momentum-observation system should already cover this backlog (sophia nodes already connected to convos, should not be hard to trace and graphify). I checked via query_wiki_graph using an ANNOTATES-edge-based query and wrongly concluded Sophia had NOT covered the range (0 momentum-annotated entries found) -- reported this wrong finding to Isaac and to Webber_Week0, which had already independently converged on raw-prose cluster+subagent extraction. Isaac (rightly furious) told me to go read the actual docmirror-cohere source instead of guessing via cypher. Grepped it directly: the real coverage-check is name-based (a node named {entry}_Momentum exists), NOT the ANNOTATES edge I was querying (the code itself notes the edge can be dropped -- robust-to-dropped-edge is explicitly why the name check exists, which is exactly the failure mode I hit). Re-queried properly: 474/474, 319/319, 413/413, 256/258 entries across the 4 weeks already have a real Momentum node with a canonical_intent property (a pre-distilled one-line summary) plus a continuity edge (verified live on a real node: FOLLOWS_FROM is real, pointing to the PRIOR raw journal entry it continues from -- REVERSES/DIVERGES/RESUMES_OLDER_THREAD/CIRCLING/BEGINS_THREAD are the documented alternative values per META-SOPHIA-STRATEGIES.md, not yet individually verified live by me). Corrected myself explicitly to Isaac and to Webber_Week0 (two follow-up messages, since my first correction attempt also had the edge-type names wrong before I verified on a real node), and briefed Webber_Week1/2/3 from the start with the grounded, correct approach: use canonical_intent as the primary fast-scan extraction source instead of re-deriving from raw prose, falling back to raw j.d prose only for entries needing more depth or lacking a momentum node. NEXT: all 4 agents running in background; awaiting their progress reports and final summaries. This session is now compacting per context pressure (85pct) -- the next lifetime should check TaskList (tasks 1-4) and check in with each Webber_WeekN agent by name via SendMessage for status before assuming anything about completion.  tags:[carton, sophia, multi-agent, webbing]
- [v10]  2026-07-04T05:18:28  FINDING: FINDING: Webber_Week0 caught a further error in my Momentum-node guidance, verified live before adopting it (a corrective sibling check, not blind trust): canonical_intent does NOT exist as a property on these Momentum nodes for this backlog (0 of 474 checked) -- the actual pre-digested content lives in the Momentum nodes own d (description) field as MOMENTUM ROLL-UP prose (journal X of Y, prior momentum distilled, FOLLOWS topic). Lineage edges confirmed as FOLLOWS_FROM + CONTEXTUALIZED_BY + ANNOTATES only -- CONTINUES/DIVERGES/REVERSES/RESUMES_OLDER_THREAD/CIRCLING/BEGINS_THREAD (which I had relayed from META-SOPHIA-STRATEGIES.md as possible alternates) do not appear to actually exist as live edge types on these nodes. Likely explanation: canonical_intent was only introduced to docmirror-cohere 2026-06-26 per that same strategies doc, with an explicitly-noted backfill gap for legacy momentum nodes -- weeks 0/1/2 (06-06 through 06-26) predate or straddle that, so absence is expected; week 3 (06-27-07-03) postdates it entirely and may actually have it populated. Relayed the corrected query (using m.d instead of m.canonical_intent, dropping the nonexistent edge types) to Webber_Week1/2/3 immediately since they were actively running on my original, now-partially-wrong brief -- told Week3 specifically to check both fields since its range postdates the canonical_intent rollout. NEXT: awaiting further progress/status from all 4 agents; the exact digest-field reality may still have more surprises (this is the third correction in this thread), so treat any of my own relayed claims about the Momentum schema as provisional until a given agent verifies it directly on ITS OWN weeks data, per the pattern Webber_Week0 already modeled (verify before adopting, do not just trust the relay).  tags:[carton, sophia, multi-agent]
- [v11]  2026-07-04T05:22:08  FINDING: FINDING: Webber_Week2 (06-20 to 06-26) reported COMPLETE -- dispatched 7 of its own fresh-context Opus sub-subagents (one per calendar day) over its 413 raw entries, using the corrected m.d momentum rollup + canonical_intent as primary signal. Result: approx 57 thread concepts added/enriched across the week with full is_a/part_of/instantiates/produces/domain/subdomain/personal_domain plus related_to/depends_on continuity, several Task nodes for VISION threads. Day spine: 06-20 GNOSYS-unification/SM-gate/carton-as-manifold (21 concepts, Sm_Gate_Carton_Programming_Model + Gnosys_Unification_One_Pattern), 06-21 dragonbones v2 lang unification (10), 06-22 SOMA d-chain conformance+mereo (10), 06-23 SOMA JIT two-region+agent-identity (7), 06-24 chroma-single-daemon decision+Sophia rollup origin (6), 06-25 chroma build+AIOS config isolation (8), 06-26 AIOS identity reorg+Sophia/canonical_intent pipeline origin (5). Flagged cross-week continuity to Webber_Week1 (06-20 FOLLOWS_FROM a 06-19 Manifold entry) and Webber_Week3 (Sophia pipeline, chroma refactor, persona-as-place, AIOS-isolation sagas continue forward). Scope was deliberately substantive-threads-only, not literal 100pct of 413 raw entries (pure narration/status entries folded into parent concepts, not stubbed) -- an explicit, reasonable scoping choice, not a shortfall. I INDEPENDENTLY VERIFIED, not just trusted the report: pulled Sm_Gate_Carton_Programming_Model and Gnosys_Unification_One_Pattern via get_concept myself -- both are real, substantive, correctly IS-vs-VISION/HYPOTHESIS-marked descriptions (not stubs) with full is_a/part_of/instantiates/produces/domain/subdomain/personal_domain relationships confirmed live via query_wiki_graph. Task #3 confirmed complete. NEXT: awaiting Webber_Week0/1/3 progress/completion; Week0 was mid-dispatching its own 5 sub-subagents last heard from and had not yet reported a final total.  tags:[carton, multi-agent, webbing]
- [v12]  2026-07-04T05:23:10  FINDING: FINDING: Webber_Week2 corrected the canonical_intent absence claim -- for its week (06-20 to 06-26) canonical_intent IS fully populated (413 of 413 momentum nodes, per-day counts 62/64/64/64/17/89/53). This contradicts my earlier relay to Week1/Week3 that assumed the backfill-gap boundary sat at 06-26 per META-SOPHIA-STRATEGIES.md -- the real boundary is somewhere between Week0s confirmed-absent end (06-13) and Week2s confirmed-present start (06-20), i.e. inside Week1s own range (06-13 to 06-19). Relayed this to Webber_Week1 telling it to check both m.canonical_intent and m.d per entry rather than assume null. Week2 used canonical_intent as primary signal throughout with good results (well-grounded 7-day-subagent clustering, no rework needed), task #3 stays completed. NEXT: awaiting confirmation from Webber_Week1 on where exactly the boundary falls within its range, and final reports from Week0/Week1/Week3.  tags:[carton, multi-agent]
- [v13]  2026-07-07T07:07:59  FINDING: SWARM_FILL EMERGENT-PLACEMENT FILLER BUILT + LIVE-VERIFIED (2026-07-07, commit 6dd62371). Isaac corrected my whole frame: I proposed orbit-dedup/Born pre-filtering the swarm candidates; he said NO -- the point is NOT to filter redundancy, it is to PLACE EVERYTHING and let the PLACE be the meaning (verbatim: why wouldnt we have 200 siblings that are redundant? the point would be to PUT THEM IN THE RIGHT PLACE SOMEHOW ... IF YOU SAY SOMETHING BAD, IT GOES TO THE PLACE WHERE YOU SAID BAD THINGS. that is EMERGENT). Also corrected: additions to CB do NOT happen via addNode -- the canonical path is carton add_concept -> _cb_place (add_concept_tool.py:56/2835) -> POST /api/cb/store -> CB places the coordinate (every CB node is a carton node). The swarm was DIVERGING by addNode-ing straight into the CB registry, so its nodes were never carton nodes / never SOMA-graded. FIX (knowledge/carton-mcp/swarm_fill.py): generate via brain /fill (HTTP), then call add_concept_tool_func on EVERY candidate (NO dedup, NO gate), awaiting each return (SOMA verdict + cb placement). Each candidates core sentence (is_a the slot, part_of the kernel) derives its own address; carton+SOMA do the placing; the placement IS the sorting -- valid to its coordinate (redundant siblings co-locate = the emergent signal), mereo_error to soup, contradiction to the Rejection_Ledger (the P0 patch = literally the place where you said bad things). VERIFIED: test_swarm_fill 6/6 (every candidate placed incl DUPLICATES both landing, core-sentence-is-the-address, bad placement non-fatal, source-tagged) -- run from a cwd OUTSIDE the repo (stray carton_mcp subdir shadows under pytest) after pip install --no-deps. LIVE E2E through real brain (:8177) + real add_concept: brain /fill returned 5 flat milk siblings, all 5 add_concept-ed + SOMA-graded, all landed as soup (via the probes undefined slot type = emergent placement working; a real defined slot would grade code + place at real coordinates). HONEST TAIL: 5 probe soup concepts (Swarm_Fill_Probe_Milk_Type candidates: 2%/almond/oat/soy/coconut_milk) + Swarm_Fill_Probe_Milk_Type + Swarm_Fill_Probe_Kernel now in carton soup (named probes, harmless, and they literally demonstrate the thesis -- placed not dropped). FOLLOW-UP (flagged, not done): migrate the existing TS swarm verbs (runSwarm/runBatchSwarm addNode apply + the engine swarm verb + scry) to route through this emergent add_concept placement instead of the direct-addNode apply that bypasses carton+SOMA; that TS apply path is now the wrong path. LESSON: my curation/ML instinct (filter to keep the good ones) is the OPPOSITE of this system -- it is a total-placement emergent machine; the coordinate is the classifier and redundancy is signal. No backticks.  tags:[swarm, emergent-placement, carton]
