# vision: doc-mirror-init (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T11:44:32  DECISION: CORRECTION (Isaac, forceful + right): I dispatched 6 raw-reader agents writing /tmp maps = random documents, illegal, non-persistent — instead of using the system. MEASURED COVERAGE GAP (the real answer to 'why is doc-mirror not current'): knowledge/carton-mcp doc(m)=0 (ACTIVE repo, never mirrored — the whole carton-kv build committed code with NO mirror = fork-on-change bypassed); treeshell/sancrev-treeshell 0; base/heaven-tree-repl 0; hooks 0; integration/conversation-ingestion 0; summarizer-mcp 0; odyssey-system 0; giint-llm-intelligence 0. Mirrored: soma-prolog 50/50, dragonbones 12/13. CA code-graph EXISTS (1311 Files/4727 Functions, repo property unset -> query by path). DECISION: route through doc-mirror-init: (1) init carton-mcp FIRST via hud-writer population (19 modules, 1:1 doc(m), closure test), seeded by the in-flight mapper outputs (drafts only, demoted from record to input); (2) then treeshell+heaven-tree-repl, hooks/ingestion, odyssey, giint; (3) read soma+dragonbones MIRRORS not source; (4) only then answer 'what carton is really for' from the doc layer, verifiably. The 6 mappers stay running per Isaac (outputs = seed material). Cursor -> phase=init.  tags:[doc-mirror-init, coverage-gap, illegal-tmp-maps, correction]
- [v2]  2026-06-10T12:39:34  FINDING: CORRECTION to the as-is catalog (Isaac challenged 'callables not stored on carton' — he was partially right; I read node_sync.py 165 lines + grepped all engines + /core myself). PRECISE TRUTH: (1) treeshell->carton: node_sync.py DOES store every rendered node as {App}_Node_{Semantic_Name} with NODE_DEF={type, options, signature, args_schema, domain} (:97-104) + is_a TreeShell_Node + collection/version/domain rels — so the node INTERFACE (incl args_schema + signature) IS on carton; ONLY the import binding (function_name/import_path/import_object/is_async/function_code) is absent from NODE_DEF (grep-verified). (2) carton->treeshell: JIT consumes options only (learnings.py parse, base.py:1329-30); callable processing (base.py:254-264,400-410) runs on family-JSON node_data only. (3) ISAAC'S MEMORY = the Add_Carton_Node_And_Sync_Functions concept (Oct2025+Jan2026 observations): add_carton_node imported code 'if callable/awaitable ref present', sync_with_carton batch-added should_be_treeshell:true concepts — grep across current engine + sancrev-treeshell + skill-manager + legacy /core + whole disk = ZERO implementations; survives ONLY as the carton concept = a THIRD D2-pattern ghost (after _compute_description_rollup and the stale ingestion-dead note): capability built-or-designed earlier, absent from running code, remembered as present. IMPLICATION: properties design = RE-IMPLEMENTING add_carton_node properly; executable gap shrinks to FOUR fields (function_name/import_path/import_object/is_async) + the JIT dict-copy; args_schema/signature/options/type already sync. The Properties_Design vision entry stands, with this narrower gap.  tags:[correction, node-sync, add-carton-node-ghost]
- [v3]  2026-06-23T05:11:30  FINDING: CORRECTION / SUPERSEDE of my 2026-06-23T04:36:51 design-of-record (which wrongly baked the CCC 'Ghost/Shell/Gear/cursor' labels in as canon). Isaac 2026-06-23: those are CybernetiCircus (CCC) vocabulary he does NOT want in the build; 'Gear' also collides with the unrelated gamification GEAR acronym (Gear/Experience/Achievements/Reality). THE STRUCTURE is right and important (Cybernet = Ghost + Shell), but USE PLAIN NAMES. THE MODEL (plain): an Agent_Identity entity = the whole agent in TWO LAYERS: (1) persona-config (structurally 'Ghost') = the persona's frame (system-prompt) + its rules — the 'who', projected into context via host/claude-p; (2) substrate (structurally 'Shell') = the carton graph (entity node+edges) + the SM state-machine (Execution_State/gate cursor) + access (skills+MCP) + the Identity_Collection (the REFLECTION STORE where observe_from_identity_pov writes the agent's observations). The persona-config is REFLECTED ONTO the substrate. What I already built (the entity, HAS_COLLECTION, Execution_State, the SM gate) IS the substrate; the persona-config (frame+rules) is the context config. PROVENANCE of the confusion: 'Ghost = AIOS = system prompt' was a SUMMARIZER CONFABULATION in Iteration_Summary_2026_06_21T01_03_52; AIOS != system-prompt (AIOS = a whole dir-system claude.md+skills+rules+assets; system-prompt is ONE part of it). Vault model fields (plain): frame, rules (persona-config) + collection, skillset, execution_state (substrate). NEXT READER: ignore the Ghost/Shell/Gear labels above this separator; use the plain two-layer model.  tags:[Correction, Cybernet, Plain_Names, Supersede]
