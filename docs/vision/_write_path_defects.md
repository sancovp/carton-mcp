# vision: Step2_Core_Done (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-09T21:38:23  FINDING: STEP 2 (a) DONE + (b) write-path blocker AUTHORIZED. (a) PURE CORE apply_carton_obj_op(description,kvobj,key_path,op{get|set|append|remove},value)+split_key_path added to carton_kv.py, 42/42 lib tests pass (dotted+indexed paths, set-creates-key, key_path=''=whole-body, ref round-trip, minimal-diff, error cases). (b) BUILDER FOUND + I VERIFIED IN CODE 2 real defects in the sanctioned n.d REPLACE write path: DEFECT 1 observation_worker_daemon.py:621-626 raw_concept branch builds concept_data WITHOUT desc_update_mode -> batch :160 c.get('desc_update_mode','append') defaults APPEND -> a replace silently APPENDS (duplicates n.d). DEFECT 2 :237-253 SET n.d CASE has 'WHEN n.d CONTAINS c.description THEN n.d' (:246) BEFORE 'ELSE c.description' (:252) and NO explicit replace branch -> a shrinking edit (remove) whose new desc is a substring of old keeps OLD n.d (silent drop). AUTHORIZED FIX (Option A, 2 one-liners, non-regressing — append default untouched so journal merge-append unaffected; only intentional replace callers change = correct semantics): (1) add 'desc_update_mode': observation_data.get('desc_update_mode','append') to the raw_concept concept_data dict :621-626; (2) insert WHEN c.update_mode='replace' THEN c.description right after the skip branch (before the CONTAINS guards) in the batch SET n.d CASE. edit_carton_obj then queues a raw_concept observation w/ desc_update_mode='replace' (sanctioned batch path; NOT the dead create_concept_in_neo4j). DEPLOY/GATE SPLIT: builder applies fix + builds edit_carton_obj + MCP tool + lib tests + commits monorepo; COMMANDER (me) does pip install --no-deps + RESTART observation_worker_daemon with env vars (daemon-needs-env-vars rule: NEO4J_URI/USER/PASSWORD + HEAVEN_DATA_DIR + GIINT_TREEKANBAN_BOARD) + reconnect_mcp carton + run the MCP-surface E2E (test concept w/ CartonObj -> edit_carton_obj set a leaf AND a remove/shrink -> confirm n.d REPLACED correctly, prose/siblings byte-identical). NOTE for Step-6 dev-flow: the coherence set now INCLUDES the observation_worker_daemon write path + the daemon restart (not just lib<->MCP).  tags:[Step2_Core_Done, Write_Path_Defects, Daemon_Fix_Authorized]
