# vision: Agent_Identity (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-23T04:13:38  DECISION: DESIGN (Isaac 2026-06-23 authorized: 'the newer actual way needs to be figured out, make it make sense however you like, that stacking is fine'). THE ACTUAL IDENTITY MODEL (supersedes identities-as-collections): an identity is an Agent_Identity ENTITY node (the actual thing), canonically named {TitleCase(handle)}_Identity (e.g. carton_identity handle 'gnosys' -> entity Gnosys_Identity, which already exists is_a Agent_Identity). The entity HAS its collection (HAS_COLLECTION -> {X}_Collection, the old Identity_Collection becomes the entity's retrieval-map/knowledge, NOT the identity itself) and HAS a frame (frame=role-prompt=persona, set by equip_persona; the gate's per-identity lifecycle/Execution_State IS the gate-state/'frame' that now attaches to the real entity). STACKS: entity=the thing; frame+collection=its parts. THE FIX it enables: the SM gate's actor resolves the raw lowercase carton_identity handle -> the Agent_Identity entity node, so _lock_into_sm_chain locks a REAL node (was no-op'ing because 'gnosys' matched nothing). IMPLEMENTATION (contained, minimal blast radius — does NOT touch the 3 equip_persona impls or heaven): sm_gate.py gets _identity_node_name(handle) (TitleCase each _-segment + ensure _Identity suffix) + resolve_identity_entity(handle, run) (MERGE the entity is_a Agent_Identity + link HAS_COLLECTION to its collection IF it exists; FAILS-OPEN); server_fastmcp.py _sm_actor() resolves the handle through it (only reached when gate ON, so default-safe). equip_persona untouched (resolution is lazy at gate-read). TEST: _identity_node_name mapping + resolve on a synthetic handle (entity MERGE'd + HAS_COLLECTION + idempotent) + the resolved entity is a usable gate actor (sm_chain_visit arms + get_active_step reads it) + read-only sanity that 'gnosys'->'Gnosys_Identity' and Gnosys_Collection exists. Reconnect-gated for live (CARTON_SM_GATE default-OFF) but the resolution code is verified by direct import. NOTE: the actual-thing/frames model still has STUBS (GNO_SYS_Identity / Gnosys_Identity_State / Relational_Identity_Pattern auto-created, undefined) — this slice canonicalizes the Agent_Identity-entity + HAS_COLLECTION half + the gate reconciliation; fleshing the frame-state model further is follow-on.  tags:[Agent_Identity, Frames, Sm_Gate, Identity_Collection, Has_Collection]
- [v2]  2026-06-23T04:18:13  FINDING: BUILT + VERIFIED the actual-identity model slice. sm_gate.py: _identity_node_name(handle) (TitleCase _-segments + ensure _Identity suffix; 'gnosys'->'Gnosys_Identity') + resolve_identity_entity(handle, run) (MERGE entity is_a Agent_Identity + HAS_COLLECTION->{X}_Collection if it exists; FAILS-OPEN). server_fastmcp.py _sm_actor(): resolves the raw carton_identity handle through resolve_identity_entity so the gate's lifecycle/Execution_State attaches to the real Agent_Identity entity (only reached when gate ON; FAILS-OPEN to the handle). VERIFIED LIVE: tests/test_identity_entity_resolution.py 4/4 (name mapping; resolve MERGEs entity+HAS_COLLECTION idempotent; the resolved entity is a usable gate actor sm_chain_visit-arms+get_active_step; real-world sanity gnosys->Gnosys_Identity + Gnosys_Collection exists) + server chokepoint probe (srv._sm_actor('zztest_srvwire_probe')->'Zztest_Srvwire_Probe_Identity'). NO REGRESSION: test_sm_core_live_wiring + test_sm_gate_e2e + test_sm_factory_live_e2e all still PASS. pip install --no-deps done; LIVE MCP needs reconnect_mcp carton (user-only) to pick it up but the gate is default-OFF so no live impact. This RESOLVES the latent 'gnosys has 0 :Wiki nodes' Core-gate finding: when the gate is enabled, _sm_actor now resolves+MERGEs Gnosys_Identity so _lock_into_sm_chain locks a real node. Equip_persona (3 impls) + heaven UNTOUCHED (resolution is lazy at gate-read).  tags:[Agent_Identity, Sm_Gate, Has_Collection, Built]
