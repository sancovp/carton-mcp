# vision: rollup-found (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T03:54:03  FINDING: DECOHERENCE POINT LOCATED PRECISELY (read code + caller-count myself). The rollup-from-relationships mechanism Isaac remembers EXISTS in CURRENT carton: _compute_description_rollup (knowledge/carton-mcp/add_concept_tool.py:1896-1936). It renders triples as SENTENCES exactly as Isaac described: '{Concept} is_a T1, T2. {Concept} part_of P1. {Concept} has_X V1, V2.' (orders is_a/part_of/instantiates first = strong-compression primitives, then rest alphabetically). Its DOCSTRING states the intended D2 design VERBATIM: 'compute the authoritative description of a concept from its triples. This function REPLACES the previous pattern of storing the caller raw prose directly in Neo4j n.d. Instead n.d is DERIVED FROM the concept relationships (triples)... raw prose preserved separately in the raw_staging field for future d-agent extraction (D1, D21).' => CONFIRMS Isaac hypothesis EXACTLY. BUT: grep '_compute_description_rollup' across carton-mcp/*.py returns ONLY the def (line 1896) = ZERO CALLERS = DEAD/UNWIRED CODE. The live write path (batch_create_concepts_neo4j) still SET n.d = caller raw prose (the old blob pattern). So NOT 'lost in migration' — a FINISHED 'D2' mechanism that was NEVER WIRED IN. That stranded function is WHY n.d is still a freeform blob instead of a triple-derived overview => the root cause of blob-preference/duplicate-concepts. THREADS TO PULL: the raw_staging field (is it wired?), the D1/D21 design series. FIX QUESTION (Isaac decision, NOT yet acted): wire _compute_description_rollup into the write path (n.d derived from triples, raw prose -> raw_staging) = the real fix; reframes CartON-KV fences entirely.  tags:[rollup-found, dead-code, decoherence-point, d2]
