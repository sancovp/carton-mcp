# vision: carton-redesign (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T04:58:56  DECISION: SESSION CONCLUSION + TOMORROW ENTRY POINT (Isaac winding down, 'do this tomorrow'). The deconfab landed a full carton description-model redesign, reasoned with Isaac: (1) THE THING = relationships(+future properties); n.d = OVERVIEW only, not the content. (2) A stranded 'D2' refactor exists: _compute_description_rollup (add_concept_tool.py:1896) renders triples as sentences ('{C} is_a X. part_of Y.') + raw_staging field (:2247) + comments asserting 'n.d must NOT be stored verbatim' (:2151) — but the rollup has ZERO callers (dead), and live n.d is still stored verbatim (Step-7 empirical). Half-wired migration whose comments lie about behavior = the root cause of blob-preference/duplicate-concepts. (3) KEY INSIGHT (Isaac): a computed n.d stored IN neo4j is REDUNDANT with the graph -> the rollup should be READ-TIME (get_concept composes it; already half-does Rels:[...]), NOT stored. (4) INTENDED FLOW: write prose to n.d (staging) -> d-agent progressively COMPILES it into the graph (entity-mentions->relationships/edges; literal scalars+arbitrary STRINGS->node PROPERTIES) -> n.d EVAPORATES -> READ = resolved rollup string of edges+props. (5) PARADOX RESOLVED: 'where do arbitrary strings live if everything's a graph?' -> PROPERTIES (a node = labels+properties+edges; properties hold literal/string KV, queryable, SET-editable). The KV system was right to want KV but wrong about WHERE = node PROPERTIES, not fences-in-n.d. Fences (CartonKvObj) = the EDIT solution (the core need, DONE); PROPERTIES add the QUERY axis (retrieve/filter/aggregate by fact, not traversal/blob-reading) = the marginal value + the cure for 're-reading everything'. TOMORROW: design+wire carton to use node properties for literals + relationships for entities + rollup-at-read; resolve the computed-vs-authored-gloss fork (Isaac leans n.d-evaporates-fully since properties absorb strings). NOT STARTED — design decision pending Isaac. (Honest: rollup-dead + n.d-verbatim CONFIRMED in code; full write-path plumbing trace NOT yet done; the redesign is reasoned/coherent, UNPROVEN — proof = wire it + watch duplication stop.)  tags:[carton-redesign, properties, rollup, tomorrow, session-conclusion]
- [v2]  2026-06-10T12:30:29  VISION: THE PROPERTIES DESIGN (counterfactual run + verdict; AWAITS ISAAC y/n — NOT decided). REBUILD PER PROGRAM: treeshell node = {n, d=gloss, ts_type/ts_function_name/ts_import_path/ts_args_schema/ts_is_async/ts_zone/ts_order props} + HAS_OPTION; JIT fetch ALREADY returns whole node so props arrive FREE — one enrichment patch (copy props into the dict) and the existing engine executes carton-born nodes (engine interprets dicts regardless of origin; zero engine change) = the Mar07 hot-neo4j-backend vision; zones=props=queryable. *Log status -> status property (stop-hook trigger = WHERE clause; the per-thing HYPOTHESIS->FINDING->DECIDED->BUILT state machine Isaac demanded 06-09 = status prop + transitions schema). GIINT lanes/done -> props (kanban move = SET). Summarizer levels -> level/processed props (kills null-n.t name-ordering). DMN -> Retrieval_Step nodes carrying cypher+order as data = retrieval protocol becomes a carton program (step-0 = insert a node not edit a CLI). Publishing registry -> unit nodes + HAS_UNIT (re-home from fence). ENABLING BUILD (finite): (1) set_property + query_by_properties + render-props-in-get_concept; (2) daemon queue rows carry properties dict through batch_create; (3) per-label schema validation AT batch_create = the write-time gate STARSYSTEM never had (reification ladder exists 3x: ONTOLOGY_SCHEMAS dict / KV-schema concepts / SOMA); (4) JIT enrichment; (5) HAS_OPTION edge-props (order/label). RECURSION-WORRY RESOLVED: targeted-edit problem was GRANULARITY (one string, many facts); one-fact-per-property -> SET is perfectly targeted; survives only for bounded JSON props (args_schema) which have NO linker exposure and can reuse apply_carton_obj_op. YESTERDAY RE-RUN: fence registry + ~5/7 KV build steps would NOT have been conceived (they are armor against n.d being shared linker-mauled prose; props are outside the warzone). SURVIVES: did-you-mean/no-stub guard (relocate to daemon stub-MERGE :322-331 = the disease site), read-time expansion (generalizes to graph transclusion), surgical prose editing. KVOBJ VERDICT: KEEP, scoped to NAMED-PARTS-OF-PROSE editor ONLY (the journal/cohered layer where description IS the medium); never the home of standalone structured data; Step-7 registry mis-homed, cheap to re-home. UNIFIED SHAPE (converges D2-rollup + Mar07-bijection + 06-09 plan): graph=links, properties=facts/parameters, n.d=gloss+prose(+fenced islands), read=composed rollup, schemas on the 3-rung ladder, validation at the daemon chokepoint. ISAAC DECISION SET: adopt design y/n; KvObj scope-limit y/n; sequencing (publishing-first locked; property tools + JIT patch are small-tier, full gate is (E)-tier — split?); default reification rung (proposal: KV-schema concepts now, SOMA at (E)).  tags:[properties, kvobj-verdict, unified-shape, decision-pending]
