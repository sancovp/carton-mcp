# vision: Regions (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-21T01:20:14  FINDING: STEP #1 (the total new carton graph shape) -- FOUNDATION, region axis VERIFIED BY ME in code (_compute_region observation_worker_daemon.py:246-278; corrects the scout which said enum=SOUP|CODE|SYSTEM_TYPE|CB and miscalled treeshell a region). THE TOTAL NEW CARTON GRAPH SHAPE: (A) ONE node label :Wiki; every concept is a :Wiki node; fields n(name/PK, Title_Case)/d(desc)/c(canonical)/t(timestamp) + region(property) + scratch properties. TYPE is expressed by IS_A edges to type-concept nodes (NOT labels) -- the ontology IS graph structure. (B) TWO ORTHOGONAL AXES (the core of the new model): VERTICAL = the PROOF REGION = a MUTABLE  property = the SOMA-verdict gradient soup->code->system_type->ont (+cb = the one non-SOMA region, out of scope this sprint). _compute_region: is_a cb_/crystal_ball ->cb; is_system_type->system_type; is_code->code; is_soup->soup; else None (no verdict -> don't clobber). ont RESERVED (SOMA doesn't surface is_ont to carton yet). Region is SCRATCH-lane (work-state reflected from verdict, NOT meaning; meaning stays in is_a/part_of). HORIZONTAL = the FOUR LENSES (Agent/Domain/Place/Code-Object) expressed as IS_A edges (e.g. is_a TreeShell_Node = the Code-Object lens). TREESHELL IS A LENS, NOT A REGION (Isaac 2026-06-19 unify-treeshell; old region='treeshell' early-return REMOVED bc it conflated the axes -- a treeshell node now gets a real vertical region from its SOMA verdict AND carries the is_a TreeShell_Node lens edge). (C) EDGES: core mereological set is_a / part_of(has_part inverse) / instantiates(instantiated_by) / related_to + domain edges (has_giint_project, has_collection, has_status, depends_on, blocked_by, maps_to). (D) PROPERTIES: two-lane doctrine (scratch lane = status/order/region/timestamps sync-direct no-SOMA; trail lane = ontology-bearing classes also emit a SOMA hasProperty_k observation; reserved keys blocked). (E) SUB-SHAPES (all the same :Wiki graph, different IS_A families): doc-mirror journal/DMN ({Repo}_{Domain}_{Subdomain}_{ts} timeline + {Repo}_{Domain}_{Subdomain} living cohered note + typed axis nodes Repo/Domain/Subdomain); starsystem WORLD-GRAPH (Seed_Ship IS_A Starsystem root -> PART_OF per-project Starsystems -> HAS_COLLECTION {ss}_Collection w/ 4 Collection_Category buckets -> Hyperclusters; kardashev Navy Starship/Squadron/Fleet PART_OF Kardashev_Map); GIINT (Project->Feature->Component->Deliverable->Task via HAS_PART/PART_OF + _Unnamed); treeshell LENS (is_a TreeShell_Node + opt_* props). (F) substrate_projector = graph->context projection (Cypher + metastack templates -> file/discord/env/skill/rule); the graph is the source, context is a projected view. SCOUT REPORT (spot-check pending for deeper lock): /tmp/carton_graph_shape_echolocation.md. STATUS: region axis IS-verified; the rest grounded in rules (world-graph-geometry, property-layer-doctrine, DMN) + scout, needs Isaac confirm before we lock + build #2 (dragonbones) on it.  tags:[Regions, Two_Axes, Lenses]
