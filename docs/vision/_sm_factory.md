# vision: State_Machine (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-21T02:18:22  DECISION: BUILD PLAN (#2, Isaac: 'lets add this stuff in dragonbones, then go back thru treeshell-lang'). GROUNDED (read compiler.py:540-669 + main.py + the nuclear rule dragonbones-must-call-library-functions): dragonbones = a PostToolUse hook over agent output -> extract_from_blocks -> compile_concepts; ROUTING (compiler.py:554-590): _strip_giint_prefix -> if a typed GIINT level (Project/Feature/Component/Deliverable/Task) route to the GIINT LIBRARY FACTORY (_sync_to_giint_registry -> llm_intelligence.carton_sync, the single webbed writer); ELSE (Skill_/Pattern_/Bug_/plain) direct add_concept. So there is exactly ONE factory today (GIINT). NUCLEAR RULE: typed ECs MUST call the library/MCP factory, NEVER add_concept directly. Isaac's correction confirmed in code: dragonbones emits cypher by calling add_concept OR factories. === SO SM EXPOSURE = the SAME shape: detect a State_Machine-typed EC -> call an SM FACTORY. The SM factory is the KEYSTONE + the missing piece (today SMs are created by raw MERGE in tests/test_sm_core_e2e.py _program; sm_gate.py has only skill_to_sm = a degenerate 1-step show-SM helper; NO generic SM constructor). PROVEN-SMALL BUILD ORDER: (1) CARTON SM FACTORY create_sm_chain in carton-mcp -- abstract the proven test_sm_core_e2e _program cypher into an idempotent function (concept -HAS_SM_CHAIN-> Sm_Chain -SM_CHAIN_RUNS{order}-> StateMachine -HAS_STEP-> Traversal_Step{required_pattern,text}) + expose as an MCP tool (so it is a callable library/MCP function per the nuclear rule); VERIFY: create an SM via it -> sm_chain_visit GATES it (stack-size rule: >1 SM = gated). (2) DRAGONBONES State_Machine EC shape + routing (compiler.py:554 seam) that CALLS create_sm_chain. (3) the skill dragonbones-carton-retrieval-state-machines. PIECE 1 (factory) is the prerequisite -> building it first. THEN (Isaac's order) revisit the treeshell-lang dragonbones DUPLICATION (lang_arg_parser + node_sync re-implement the untyped path -> carton queue, BYPASS the factory routing) -> fix = treeshell annotate-on-add routes typed annotations through the SAME factories. PROTOCOL A (new build, closeable spec): dispatch piece-1 build w/ tight prompt grounded in test_sm_core_e2e.py + sm_gate.py; I verify E2E.  tags:[State_Machine, Sm_Factory, Factory_Routing]
- [v2]  2026-06-21T02:23:36  FINDING: PIECE 1 DONE + VERIFIED BY ME + COMMITTED (b200dd2). create_sm_chain added to carton-mcp/sm_gate.py (after skill_to_sm): idempotent generic SM factory -- concept -HAS_SM_CHAIN-> Sm_Chain -SM_CHAIN_RUNS{order}-> State_Machine -HAS_STEP-> Traversal_Step{required_pattern,text} + NEXT_STEP; uses the module T_*/R_* constants; abstracts the proven test_sm_core_e2e _program cypher; returns {concept, sm_chain, sms, steps, gated:len>1}. VERIFICATION (I ran it myself, not the agent's word): tests/test_sm_factory_e2e.py 3/3 PASS exit 0 -- (1) structure queried back correct, (2) sm_chain_visit ARMS require_next + locks at the gating step on the factory-built SM = it genuinely GATES (not just nodes), (3) idempotent (double-call same counts). Self-cleaning (Zztest_Factory_ prefix). pip install --no-deps carton-mcp done (function importable). I read create_sm_chain source myself (sm_gate.py:470) -- matches design. Note: NOT yet exposed as an MCP tool (library function only, per spec scope; add the MCP tool when dragonbones/treeshell need the agent-callable surface). NEXT = PIECE 2: dragonbones State_Machine EC + routing (compiler.py:554 seam) that CALLS create_sm_chain -- requires designing the SM EC SYNTAX (agent-facing contract; ground in parser.py for how steps/nested-claims express; propose a concrete example EC for Isaac's nod before wiring). THEN piece 3 (skill) + the treeshell-lang dragonbones-duplication fix (Isaac's order).  tags:[State_Machine, Sm_Factory, Verified]
- [v3]  2026-06-21T03:33:56  FINDING: PIECE 2A DONE + VERIFIED BY ME + COMMITTED (c423e37). create_sm_chain_live(concept_name, state_machines, sm_chain_name) added to carton-mcp/sm_gate.py: self-contained library entry (opens own KnowledgeGraphBuilder from env via the proven _conn/_mk_run pattern, calls create_sm_chain) = the function dragonbones imports per the nuclear rule (caller passes only the spec, never a connection). VERIFIED (I ran it): tests/test_sm_factory_live_e2e.py 2/2 PASS exit 0; importable from installed pkg. === NOW PIECE 2B (dragonbones SM EC, approach A) -- KEY SEMANTIC nailed: to actually GATE, a Sm_Chain needs >1 SM (stack-size rule: a lone SM is OFF/show-only). SO the SM EC declares the GATING steps, and the dragonbones handler builds the STANDARD 2-SM STACK: order-0 = auto show-SM ({gated}_Show, 1 step, no pattern) + order-1 = the gating-SM (the EC concept_name) with the declared steps -> create_sm_chain_live(concept=<gates target>, [show_sm, gating_sm]) = a real gate. (Design choice, state-what-is: auto-show-SM = natural show-then-gate; adjustable.) SM EC SYNTAX A: operator + <SM name> {is_a=State_Machine, gates=<Concept_X>} then one claim per step {step=<id>, required_pattern=<regex>, text=<...>, next=<id>}. Parser change = additively preserve per-claim KV groups (a claims field) so the handler reads step-fields as PROPERTIES (bypassing relationship-flatten). Dispatching 2B w/ full semantics; verify E2E (SM EC string -> parse -> compile -> carton has GATING Sm_Chain on the gated concept).  tags:[Verified, Sm_Factory]
