# vision: Linker_Corrupts_Fences_Live (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-09T21:50:31  FINDING: GATE FINDING (read raw n.d from neo4j myself, ground truth): the LIVE auto-linker (auto_link_description via observation_worker_daemon linker_thread) CORRUPTS stored CartonObj fences — confirmed worse than expected. On Carton_Kv_Gate_Test (a concept created via add_concept with two CartonObj fences, lowercase bodies), the stored n.d shows: (1) ~EVERY word linkified into [w](../W/W_itself.md), not just Title_Case — it fuzzy-matched common words for/free/repo/tier/prose/steps/install + even the OPEN TAG (name="reg" -> [name](../Name/Name_itself.md)="reg"); (2) JSON STRUCTURE DESTROYED — ["clone","install"] -> "clone", "[install](...)" and "y": [10,20] -> "y": 10, 20 (the array brackets [ ] consumed, because [text] is markdown-link syntax the linker's regex eats). So a CartonObj stored THROUGH add_concept is unparseable. CONSEQUENCE (re-sequence): STEP 2's FULL round-trip E2E (store a CartonObj via the MCP -> edit_carton_obj it) is BLOCKED until STEP 3 (linker fence-opacity). The lib edit_carton_obj is correct (8/8 + my run); the daemon REPLACE FIX is independently verifiable (Carton_Kv_Replace_Test). NEW GATE PLAN: Step 2 = lib-correct + daemon-replace-fix-verified (queued ALPHA-replace test, discriminator = absence of 'GAMMA DELTA' in n.d); then DO STEP 3 NEXT, then a COMBINED stored-fence E2E (create concept w/ CartonObj -> fence survives intact -> edit_carton_obj round-trips) gates BOTH 2 and 3. This also RAISES Step 3's importance: fence-opacity must mask the WHOLE fence (open tag + body) from the linker, and the linker eating [ ] brackets means even non-Title_Case content is unsafe.  tags:[Linker_Corrupts_Fences_Live, Step3_Is_Prereq, Resequence]
