# vision: Step4_Passed (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-09T22:41:27  FINDING: COMBINED STEP 4+4B GATE — 5 of 6 sub-checks PASS E2E (commander-verified, raw neo4j + MCP): (1) schema concept auto-types IS_A Carton_Kv_Schema + USES_KV_SCHEMA edge + schema fence intact; (2) validate_carton_obj(Valid)=VALID; (3) validate_carton_obj(Invalid)=INVALID with exact bad keys ('tier' required + count not integer); (4) edit set unknown bare ref = REFUSED no-stib + neo4j confirms NO stub node created (the 36.5k-disease cure proven); (5) fence-PRESERVATION = add_concept(replace, prose-only) carried BOTH fences forward verbatim. ONE FAILURE: (6) remove_fence E2E — second fence SURVIVES (not removed). ISOLATED: lib carry_forward_fences PROVEN correct in isolation (removed=['second']->no second; removed=[]->carries); the queued obs is CORRECT (removed_fences=['second'], desc without second, processed); re-tested on FRESH daemons (post-4B-install, new in-memory code) -> STILL fails. So NOT stale-daemon — a real DAEMON-RUNTIME bug in removed_fences threading (it's not reaching carry_forward at runtime, OR a duplicate/re-processing re-adds second). Threading code LOOKS correct (concept_rows :161, call :245, raw_concept :661) — needs a debug trace. HANDED to builder. Step-4 (#5) COMPLETE; #9 (4B) preservation done, remove_fence pending the fix. DAEMON-HYGIENE NOTE: multiple observation_worker_daemons auto-respawn (ensure_worker_running); a stale pre-4B-install daemon (27571, cwd source, old in-memory code) was racing the queue earlier (now dead). LESSON: a pkill+launch in ONE bash command self-kills (the un-bracketed launch name matches the pkill regex over my own shell) -> exit 144; never combine pkill + the daemon launch in one command.  tags:[Step4_Passed, Remove_Fence_Daemon_Bug, Gate_Caught_It]
