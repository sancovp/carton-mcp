# vision: obstruction-list (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T12:57:29  FINDING: THE OBSTRUCTION LIST (Isaac-demanded: exact code causing the slowness where every AI relearns the old workarounds). Read learnings.py IN FULL (the carton<->treeshell module: semantic_to_carton naming, get_node_definition = the 'source-of-truth flip' per its own docstring, get_node_learnings = JIT learning-injection into every render via renderer.py:87-93, add_node_learning writeback; sync writes at renderer.py:189). COMPLETE LOOP NOW READ FIRSTHAND. THE LIST: (1) NO PROPERTY SURFACE — server_fastmcp 26 tools none touch properties; daemon writes fixed fields only -> every updating structure exiled to side-stores -> AI must learn ~8 storage systems instead of 1 [ROOT ITEM]. (2) READ-BACK DISCARDS 4/5 FIELDS — base.py:1329-30 uses ONLY options from the parsed NODE_DEF (type/signature/args_schema/domain thrown away) -> source-of-truth flip looks done write-side, silently truncated read-side. (3) SYNC OMITS FUNCTION BINDING — node_sync.py:97-103 NODE_DEF lacks function_name/import_path/import_object/is_async while meta_operations.py:439-450 writes exactly those to family JSON -> carton copy permanently non-executable, JSON always wins. (4) JIT UNDERSTANDS ONE EDGE TYPE — base.py:1283-91 HAS_OPTION only, all other rels fetched+discarded -> graph hierarchy doesn't project into nav. (5) STUB SPAWNER — daemon:322-331 MERGE auto-creates stubs -> garbage graph -> AIs distrust it -> back to blobs. (6) CODE LIES — _compute_description_rollup :1896 zero callers + :2151 comment claims n.d not stored verbatim while live path stores verbatim -> false model for every reader. (7) NAMES AS DATABASE — identity=MERGE on name; fields packed into name strings everywhere (journal ts, timeline, GIINT prefixes, summarizer level-by-prefix, n.t null on Iteration_Summary) -> per-subsystem private encodings, rename=identity loss. (8) NO SYNCHRONOUS WRITE — queue+daemon only, no read-after-write -> the REAL reason kanban went sqlite; migration needs a direct sync SET path for properties (safe: property-sets don't touch n.d/linker). (9) THE SIDE-STORES (consequence): treekanban sqlite :10, automations JSON :15-18 (incl code_pointer callables), builders/course JSON. (10) chroma cache NameError :2675 (2-tool RAG never worked) + enforcement thread commented :161-162. ISAAC'S 3 QUESTIONS ANSWERED YES/YES/YES: properties could absorb nearly all storage (caveats #1 #8); the old programming patterns were workarounds for missing properties; KvObj = string-edit convenience + guards, nothing more. DISSOLVING BUILD = property tools + SYNC property-write path + daemon property support + schema-at-write-gate + JIT builds the _meta_add_node node_data dict from concepts (items 2,3 become few-line fixes once properties exist).  tags:[obstruction-list, learnings-module, source-of-truth-flip, sync-write-gap]
