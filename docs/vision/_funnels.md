# vision: Treeshell (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-21T01:41:46  DECISION: EXTENSION (Isaac 2026-06-21): TREESHELL-PER-LENS-AGENT + the PUBLISHING LOOP. Isaac's intuition + conclusion, both true for different reasons: OPERATING level -> only the AGENT lens needs a treeshell to OPERATE (it runs workflows = navigates a command tree); the Domain-webber just webs facts (carton tools), the Evolver edits code/node-defs (carton + bash + read/write) -- neither needs a command-tree to do its job. PUBLISHING level -> ALL THREE lens-agents get their OWN individual treeshell anyway, because THE TREESHELL IS THE PUBLISHED SURFACE (the agent's public wiki + funnel); that is HOW WE CLOSE THE PUBLISHING LOOP (Isaac). THE RULE: treeshell-to-operate = Agent only; treeshell-to-publish = all three; since every agent gets published, every lens-agent gets its own treeshell. PUBLISHING TYPES (Isaac): any PUBLIC agent has a public wiki + everything; TWO TYPES = PROPRIETARY vs OPEN-SOURCE; 'we make the funnels differently' = two funnel templates over the same published-treeshell surface (open-source rides tier-2 public repo + tier-3 marketplace/wiki per scalable-publishing-system; proprietary = the gated funnel). IS vs VISION: IS = the treeshell<->carton backend (node-defs live in carton, projected out, json-off toggle, built+E2E 2026-06-19) -- a treeshell CAN already be carton-native + published from the graph. VISION = a published treeshell-wiki per lens-agent + the two funnel types = the publishing-loop build. SIDE-EFFECT (resolves a tension): 'treeshell = the Code-Object lens' (a node rendering) vs 'each agent has its own treeshell' (command/publish surface) = the SAME thing at different scale -- an agent's treeshell is a graph of Code-Object-lens nodes = exactly its publishable wiki. (Prescan corroboration to check later: Treeshell_Family_Agent_Management, Skill_Understand_Seed_Publishing.)  tags:[Treeshell, Publishing, Funnels]
