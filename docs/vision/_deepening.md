# vision: coherence-cron (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T19:45:43  VISION: ISAAC NOTE-TO-THINK-ABOUT (2026-06-10, near-verbatim — NOT a build order): THE GRAPH-COHERENCE CRON. 'We should start thinking about the cron that coheres the graph — look how much stuff we have going; we are going to want to look at everything that got added to the graph probably DAILY and roll it up — or maybe every few hours, or even every 10 minutes, I dont know. The idea: run a MINIMAX AGENT that feeds itself the most recent carton concepts THRU A FILTERED FUNCTION TOOL that catches whatever is being pulled, CHECKS IF ITS BEEN DEEPENED ALREADY before and discards it, then PROCESSES it and uses another WRAPPED TOOL (wrapped add_concept schema) so when it gets deepened it adds it to the BLACKLIST.' DESIGN OBSERVATIONS (mine, for when this gets worked): (1) the blacklist is FREE with the property layer — deepened_at timestamp property on the node IS the blacklist (filter = query_by_properties/cypher WHERE deepened_at IS NULL); no separate list structure needed. (2) The filtered-pull tool = get_recent_concepts/timeline scoped + the deepened_at filter, server-side so the agent cannot pull wrong. (3) The wrapped add_concept = schema-enforced deepening writes (tool-side structure, agent-side semantics — the create_subphase_summary lesson). (4) This is ANOTHER INSTANCE of the WD-organ pattern proven today by the blog organ: cron -> minimax agent (heaven dispatch, journal_agent_config) -> wrapped tools -> property flip; cadence configurable in the automation JSON. (5) Its natural prey = the stub disease (auto-created empty nodes like the Journeycore_* stubs) + same-day rollup of the dozens of journal/timeline nodes a working day generates — the agent-side rebirth of the dead _compute_description_rollup intent. (6) Relates to the PARKED detection question (both are standing WD routines watching the graph; could share the pull/filter machinery). STATUS: VISION, journaled to think about — no build until Isaac says go.  tags:[coherence-cron, deepening, minimax, blacklist, rollup]
