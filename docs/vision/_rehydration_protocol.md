# vision: Dmn (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-09T23:10:41  VISION: DMN IMPROVEMENT (deferred — do AFTER the current carton-kv build finishes): make the MANDATORY FIRST STEP of every DMN rehydration the CHRONOLOGICAL TIMELINE query, NOT a named-note guess. The failure (caught by Isaac 2026-06-09): post-compact I cherry-picked ~7 cohered notes BY NAME GUESS (just the active build thread) and missed that the whole day was one connected web. The fix: step 0 of DMN = ask the net 'SINCE THE LAST TIME DMN WAS USED, WHAT JOURNAL ENTRIES WERE ADDED, IN WHAT ORDER, OF WHAT KIND (vision/finding/decision/intent = what they are ABOUT impl-vs-vision-wise), AND WHAT TAGS/coordinates they carry.' The exact query: MATCH (e)-[:INSTANTIATES]->(:Wiki{n:'Doc_Mirror_Journal_Entry'}) ORDER BY e.t DESC LIMIT N, OPTIONAL MATCH its IS_A Doc_Mirror_Journal_{kind}, its PART_OF subdomain/domain, its RELATED_TO tags; RETURN ts,kind,domain,subdomain,collect(tags) ORDER BY ts DESC. This gives the agent the WHOLE recent web (every thread, in order, what each is about) BEFORE it drills any single thread — so it can see e.g. that a build thread is actually the keystone of a larger arc, instead of rehydrating one branch in isolation. ONLY AFTER this timeline overview do the existing recursive steps (#7 tree, #8 state, #3 by subdomain/tag, #6 neighborhood) run. Wire it into the doc-mirror-memory-net SKILL as 'Step 0 / lookup #0 — timeline-since-last-DMN' above the current #1 locate (which only gives the single latest entry — too thin to be step 1). Composes with conversations-must-be-continuable + the recursive-rehydration NON-NEGOTIABLE already in the skill.  tags:[Dmn, Read_Layer, Rehydration_Protocol, Timeline_Since_Last_Use, First_Step]
