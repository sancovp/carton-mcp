# vision: swarm (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-07-07T07:07:59  FINDING: SWARM_FILL EMERGENT-PLACEMENT FILLER BUILT + LIVE-VERIFIED (2026-07-07, commit 6dd62371). Isaac corrected my whole frame: I proposed orbit-dedup/Born pre-filtering the swarm candidates; he said NO -- the point is NOT to filter redundancy, it is to PLACE EVERYTHING and let the PLACE be the meaning (verbatim: why wouldnt we have 200 siblings that are redundant? the point would be to PUT THEM IN THE RIGHT PLACE SOMEHOW ... IF YOU SAY SOMETHING BAD, IT GOES TO THE PLACE WHERE YOU SAID BAD THINGS. that is EMERGENT). Also corrected: additions to CB do NOT happen via addNode -- the canonical path is carton add_concept -> _cb_place (add_concept_tool.py:56/2835) -> POST /api/cb/store -> CB places the coordinate (every CB node is a carton node). The swarm was DIVERGING by addNode-ing straight into the CB registry, so its nodes were never carton nodes / never SOMA-graded. FIX (knowledge/carton-mcp/swarm_fill.py): generate via brain /fill (HTTP), then call add_concept_tool_func on EVERY candidate (NO dedup, NO gate), awaiting each return (SOMA verdict + cb placement). Each candidates core sentence (is_a the slot, part_of the kernel) derives its own address; carton+SOMA do the placing; the placement IS the sorting -- valid to its coordinate (redundant siblings co-locate = the emergent signal), mereo_error to soup, contradiction to the Rejection_Ledger (the P0 patch = literally the place where you said bad things). VERIFIED: test_swarm_fill 6/6 (every candidate placed incl DUPLICATES both landing, core-sentence-is-the-address, bad placement non-fatal, source-tagged) -- run from a cwd OUTSIDE the repo (stray carton_mcp subdir shadows under pytest) after pip install --no-deps. LIVE E2E through real brain (:8177) + real add_concept: brain /fill returned 5 flat milk siblings, all 5 add_concept-ed + SOMA-graded, all landed as soup (via the probes undefined slot type = emergent placement working; a real defined slot would grade code + place at real coordinates). HONEST TAIL: 5 probe soup concepts (Swarm_Fill_Probe_Milk_Type candidates: 2%/almond/oat/soy/coconut_milk) + Swarm_Fill_Probe_Milk_Type + Swarm_Fill_Probe_Kernel now in carton soup (named probes, harmless, and they literally demonstrate the thesis -- placed not dropped). FOLLOW-UP (flagged, not done): migrate the existing TS swarm verbs (runSwarm/runBatchSwarm addNode apply + the engine swarm verb + scry) to route through this emergent add_concept placement instead of the direct-addNode apply that bypasses carton+SOMA; that TS apply path is now the wrong path. LESSON: my curation/ML instinct (filter to keep the good ones) is the OPPOSITE of this system -- it is a total-placement emergent machine; the coordinate is the classifier and redundancy is signal. No backticks.  tags:[swarm, emergent-placement, carton]
- [v2]  2026-07-07T08:14:52  DECISION: CB SWARM ARC TABLED (Isaac 2026-07-07). The placement question surfaced the real architecture: add_concept is Python-import-only (no HTTP surface), which forces the lifter to be Python next to carton; the clean fix is to give carton add_concept a thin HTTP endpoint -- which IS making carton a SaaS (discussed before, and how it would be). Isaac: right shape, but a real build with real per-model-call cost, and not wanted right now. Easier CB-fill route he named: send a bunch of minimax ralphs WITH carton to inspect everything (agents with carton tools filling, not the 1-shot lift-and-place approach). DECISION: table the CB-swarm emergent-placement fill; move to CICD. STATE LEFT (resumable): brain-agent bridge into CB is DONE + live (base/brain-agent vendored, brain /fill wired into runSwarm+runBatchSwarm+scry, server on :8177). swarm_fill.py (emergent add_concept-every-candidate filler) is BUILT + tested 6/6 + live-E2E-verified but PROVISIONALLY placed in carton-mcp (marked in its docstring) -- decided home is CB once carton exposes add_concept over HTTP. The TS swarm verbs still use the old addNode apply (bypasses carton and SOMA) -- unchanged, flagged. RESUME TRIGGER: when building carton SaaS (the HTTP add_concept endpoint), move swarm_fill into CB (two HTTP calls: brain fill then carton add_concept), retire the TS addNode apply. Brain server on :8177 can be stopped while tabled. No backticks.  tags:[swarm, tabled, carton-saas]
