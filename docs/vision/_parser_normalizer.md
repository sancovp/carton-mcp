# vision: Step1_Verified (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-09T21:30:00  FINDING: STEP 1 GATE PASSED (commander-verified myself, not the report): ran python3 test_carton_kv.py -> 26/26 pass under my hand; read all 302 lines of carton_kv.py. CORRECT: scan_json_span = real string-context-aware brace/bracket depth scanner (skips strings + backslash escapes; NOT regex .*?) so a literal </CartonObj> inside a quoted value is harmless; refs_to_strict_json tracks string context (bare Title_Underscore -> {$ref} ; quoted literals + true/false/null untouched); body_from_obj inverse; find_carton_objs = open-tag regex (no > in attrs) + span-scan body + explicit close-tag check, skips-malformed-by-default; replace_carton_obj_body splices ONLY body_span by original offsets = byte-identical prose/siblings (minimal-diff basis for edit_carton_obj). PLACEMENT DECISION (approved): builder put it in a NEW module carton_kv.py (pure stdlib, no neo4j/MCP) instead of inside the 87k-line carton_utils.py — onion-clean, standalone-testable, zero blast radius (Build_Plan said '(carton_utils)' loosely; separate module is better; carton_utils can re-export later if needed). Nothing imports it yet. Task #2 complete. CLEARED builder to STEP 2 (edit_carton_obj lib fn + first MCP tool) — Step 2 is the FIRST step with a real MCP-surface E2E gate (call the tool after pip --no-deps + reconnect_mcp carton, confirm one-leaf edit leaves everything else byte-identical).  tags:[Step1_Verified, Carton_Kv_Module, Parser_Normalizer]
