# vision: abstraction-cypher (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T17:54:26  DECISION: ISAAC RATIFIED + EXTENDED the integration (2026-06-10): (1) EQUIVALENCE (Isaac verbatim sense): 'a state machine is really the same as a skill... these composed extended state machines are skillchains. Its equivalent... a SM could literally say pull this concept which has a path on it and go read whats in the path — thats a skill, literally a stupid version of it.' This matches the 06-04 finding exactly (links=skills; StateConfig(goal,tools,prompt_suffix)=a skill; composite = orchestrator-wraps-inner turtles-all-the-way-up, heaven_base/state_machine.py:12-13 re-grounded today). CANON: skill=state, skillchain=state machine, composite skillchain=orchestrator-wrapping-inner. (2) SKILLS MIRRORED VIA PROJECTION: put all skills as concepts projected out via substrate_projector (already proven capability) — even if initially just for PUBLISHING (each skill package shipped with CI/CD as a plugin in the marketplace); with cd37c66 props-merge, skill frontmatter=properties + body=render. (3) ARCHITECTURE DECISION (Isaac): abstraction-cypher does NOT become its own thing next to carton — 'we wanna marry them so this is part of carton entirely. Because it doesnt do anything that is anti-cartonly.' CONSEQUENCE: flow nodes (TraversalStep-like) become ordinary carton concepts in the same graph (journaled/linked/RAG-able/property-tracked); the GATE moves from cypher-text regex to CARTON'S OWN TOOL SURFACE (set_properties/add_concept/query refused unless the active step's condition is met) — which RESOLVES the key gap flagged at 17:48 (agents act through carton tools, not raw cypher); the toy's carton-foreign parts (own driver, own labels, :Wiki write-block) dissolve. (4) Heaven KeywordBasedStateMachine + compoctopus StateMachine(GenericS)/make_compiler_sm = the already-built agent-side SMs (minimax agents can carry them); the carton flow layer is the GRAPH-side of the same machine. STEP 3 RE-SCOPED: Retrieval_As_Data = build the carton-native flow layer (flow-as-concepts + tool-surface gating + cursor-as-edge + weighted transitions), porting abstraction-cypher's mechanics INTO carton-mcp rather than running its MCP beside it.  tags:[abstraction-cypher, skill-sm-equivalence, marriage-into-carton, compoctopus]
