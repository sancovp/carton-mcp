# vision: carton (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T03:45:13  FINDING: HYPOTHESIS CONFIRMED in code (I read the legacy file myself, Isaac authorized /core/ for this). LEGACY markdown/github-mirror-era add_concept_tool_func (/home/GOD/core/computer_use_demo/tools/base/tools/add_concept_tool.py:94-172) ROLLED UP relationships into a composed concept document: (1) main {Concept}.md = '# Title' + '## Overview'(description) + '## Relationships' with a '### {rel_type} Relationships' section per relationship listing items (:152-166); (2) per-relationship-type component files components/{rel_type}/{Concept}_{rel_type}.md with each related concept as a clickable GITHUB LINK '[{item}]({url})' (:131-145) = navigable rollup; (3) a concept = a structured DIRECTORY concepts/{Concept}/components/{rel_type}/ (:118-122). ORGANIZING PRINCIPLE = a concept's rendered form was prose-Overview + relationships-rolled-up-as-linked-sections; the description was only the Overview SLICE of a graph-composed document, never freeform-everything. DECOHERENCE = neo4j migration kept ONLY the bare Overview string as n.d; relationships -> separate edges; get_concept appends a FLAT Rels:[...] at read, NOT the structured rolled-up linked document. The composition-as-organizing-principle was never ported to neo4j. (CONFIRMED: legacy rollup, read directly. NOT-yet-pinned: the exact migration commit that dropped it. Current write path stores n.d = raw description = composition gone.) IMPLICATION: the blob-preference / un-editable-description / duplicate-concepts failure is a REGRESSION from this lost rollup; CartON-KV fences = re-inventing by hand the graph-composition carton used to do natively. NEXT: pin the migration commit + decide whether to RESTORE rollup-composition (get_concept composes description FROM relationships, navigable) as the real fix vs the fence workaround.  tags:[carton, rollup-confirmed, decoherence, deconfabulate]
