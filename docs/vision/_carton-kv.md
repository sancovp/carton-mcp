# vision: carton-kv (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T01:41:37  FINDING: REHYDRATED carton-kv build fully (Compact_Anchor 22:53 + Build_Progress + Locked_Spec + Build_Plan) after a Claude Code shutdown crash at a compact boundary. FRONTIER CORRECTION: docmirror-cursor was STALE — it read Memory_Net/Dmn_First_Step (updated 19:31, the PV2 publishing thread) but the real last work was the carton-kv build (21:13->22:53), which advanced the cohered notes WITHOUT advancing the cursor. Isaac-confirmed frontier = Step 5 (ref-expansion in get_concept) BUILT + lib-verified 61/61 (commit 8de0c7c), gate was mid-run at 84%. Resuming the Step-5 E2E gate now per the Compact_Anchor: 4 get_concept ref-expansion checks (Kv_Exp_Top->Mid->Leaf chain markers TOP/MID/LEAFMARK at depth 0/1/2 + Kv_Exp_A<->B cycle at depth 3, must terminate).  tags:[carton-kv, step5, ref-expansion, cursor-stale]
- [v2]  2026-06-10T01:42:34  FINDING: STEP 5 GATE PASSED — commander-verified E2E myself through the real mcp__carton__get_concept MCP surface (not a report). All 4 checks pass on the pre-made test concepts: (1) get_concept(Kv_Exp_Top, expand_refs=False, depth=0) = RAW, bare Kv_Exp_Mid, no MIDMARK; (2) depth=1 = MIDMARK present, LEAFMARK ABSENT (one hop only); (3) depth=2 = LEAFMARK present (recurses 2 levels); (4) get_concept(Kv_Exp_A, expand_refs=True, depth=3) over the A<->B cycle = expands A->B->A->B then TERMINATES with an explicit '…cycle⟩' guard marker, NO HANG. Expansion is render-only (stored n.d unchanged), wraps target as ref⟨desc ‖ rels⟩, cycle-guarded per Locked_Spec. Steps 1-5 of the carton-kv build now ALL DONE + commander-gated E2E. NEXT = Step 6 (edit-carton-kv dev-flow skill + use-rule) then Step 7 (publishing unit-registry as the first real CartonObj). Deferred: clean test pollution (Kv_Exp_*, Carton_Kv_* test concepts, stubs Test_Concept/Test_Pattern/Cave_Repo/A_Unit/B_Unit) after Step 6/7 land.  tags:[carton-kv, step5, gate-passed, ref-expansion]
- [v3]  2026-06-10T02:40:11  DECISION: STEP 6 PLACEMENT DECISION (commander): edit-carton-kv is a dev-flow for the CARTON-MCP repo, NOT for doc-mirror itself. Per every-build-ends-in-a-development-flow-skill ('a rule scoped to the project the thing lives in'), it goes SCOPED TO carton-mcp: skill -> knowledge/carton-mcp/.claude/skills/edit-carton-kv/SKILL.md ; use-rule -> knowledge/carton-mcp/.claude/rules/editing-carton-kv-uses-the-dev-flow.md. ALTERNATIVE considered + REJECTED: placing it in doc-mirror-system/.claude/skills/ alongside edit-the-read-layer/edit-the-journal-schema — rejected because THOSE are doc-mirror's OWN dev-flows; edit-carton-kv belongs WITH the code it documents (carton-mcp). This SETS THE CONVENTION for all future per-repo dev-flow skills: they live in their own repo's .claude/, not doc-mirror-system's. Dispatching a builder to author both artifacts, grounded in CURRENT code (boundary map predates Steps1-5 so line refs drifted), following the edit-the-read-layer 3-part shape; commander verifies the artifact E2E.  tags:[carton-kv, step6, dev-flow-skill, placement]
- [v4]  2026-06-10T02:47:27  FINDING: STEP 6 DONE + commander-verified the ARTIFACT against current code (not the builder report). edit-carton-kv dev-flow skill (150 lines) + use-rule (29 lines) written at knowledge/carton-mcp/.claude/{skills/edit-carton-kv/SKILL.md,rules/editing-carton-kv-uses-the-dev-flow.md}, committed d4af0e8 (not pushed). Spot-checked 3 load-bearing claims E2E against code, ALL accurate: (1) auto_link_description fence-opacity add_concept_tool.py:386 masks chr(0xE000+i):412 / restores :418-419; (2) BOTH daemon parse paths forward removed_fences — main loop :486 + raw_concept :662 (the canonical remove_fence-bug coherence point); (3) get_concept ref-expansion render-only server_fastmcp.py:1326-1329 (expand_refs and depth>0 only, never mutates n.d). Builder honestly corrected hints: remove_fence is a wrapper-level op (carton_utils.edit_carton_obj:95-104) NOT a lib op (_VALID_OPS=get/set/append/remove); both parse paths ALREADY forward removed_fences in current code (bug fixed both, regression test test_parse_queue_file_forwards_removed_fences). HONEST CAVEATS: (a) skill correctness = ACCURACY-verified (a knowledge/dev-flow skill, no RELIABILITY block per the model); the full 'different agent uses it E2E' proof comes on first real carton-kv edit via the use-rule. (b) container-DISCOVERABILITY follow-up: skill lives at knowledge/carton-mcp/.claude/skills/ — loads when cwd is at/under carton-mcp; may need equipping into the active skills set for global discovery (persona-sync may clobber a symlink) — NOT yet done. NEXT = Step 7 (publishing unit-registry as the first real CartonObj -> returns to the parked publishing backbone).  tags:[carton-kv, step6, dev-flow-skill, done]
- [v5]  2026-06-10T03:05:28  FINDING: STEP 7 DONE + commander-verified E2E through the real MCP surface. FIRST real (non-test) CartonObj built: Publishing_Unit_Schema (is_schema CartonObj, json-schema) + Publishing_Unit_Registry (CartonObj schema=Publishing_Unit_Schema, the 6 scalable-publishing tier-2 units mirroring publish-manifest.json 1:1). VERIFIED: (1) registry fence stored BYTE-INTACT (6 units, JSON arrays/booleans/nested objects preserved = fence-opacity live on today's daemon PID 64363, started ~01:11 post-Jun9-work); (2) validate_carton_obj(Publishing_Unit_Registry, registry) = VALID against the schema; (3) Publishing_Unit_Schema auto-typed IS_A Carton_Kv_Schema (is_schema=true auto-typing); (4) schema-graph edges wired both ways (uses_kv_schema / used_by_kv); (5) schema ref resolves to real concept, NO stub. GROUNDED, NON-DESTRUCTIVE: mirrors the EXISTING publish-manifest.json (no invented schema); the JSON stays source of truth. ===CARTON-KV BUILD COMPLETE (Steps 1-7 all DONE + commander-gated E2E)===. DEFERRED DECISIONS (Isaac): (a) MIRROR-vs-REPLACE — promote the registry CartonObj to source-of-truth replacing publish-manifest.json, or keep it a mirror? (b) refs-in-registry — v1 is pure-literal-data; a richer version would make each unit a bare ref to a per-unit concept (dogfoods ref-expansion on the real artifact); (c) test-pollution cleanup: Kv_Exp_*, Carton_Kv_* test concepts + stubs Test_Concept/Test_Pattern/Cave_Repo/A_Unit/B_Unit + my minor new is_a/instantiates stubs (Publishing_Registry, Json_Schema). NEXT FRONTIER: the parked publishing backbone (PV2 / Publishing_Strategy: reconcile monorepo-org <-> publishing-system, auto-publish to aisaac; entry = #52 PV2-0 dev-clone-divergence).  tags:[carton-kv, step7, first-real-cartonobj, build-complete]
