# vision: sancrev-read-complete (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T12:45:00  FINDING: SANCREV-TREESHELL READ 100% BY ME (all 8 modules, 2346 lines: treekanban/mcp_server/treeshell_functions/persona_manager/persona_fns/automation_fns/crystal_forest/__init__). THE FINDING THAT ANSWERS ISAAC'S REAL QUESTION (carton must program EVERYTHING — lists/todolists/updating structures — via triples; treeshell was an EXAMPLE): every updating information structure in the system was EXILED from carton into a side-store: TreeKanban todolist = SQLITE (HeavenBMLSQLiteClient; cards {title,lane,priority,tags,desc}; carton only resolves Starsystem HAS_PATH :246-253); automations = JSON files (/tmp/heaven_data/automations/, AutomationSchema {name,schedule,code_pointer,code_args,prompt_template,priority,tags,enabled} — NOTE code_pointer = callable-binding-in-a-record, already exists in JSON-store form, fired by CAVE); game/builder state (sancrev/PAIA/SANCTUM/CAVE) = per-builder JSON stores; omnisanc/course state = .sancrev_state/.course_state JSON; equipped/personas/agents = JSON. Treeshell today = navigation over a FEDERATION of ~8 disconnected side-stores, each with wrapper fns + family JSON; carton = only the knowledge/links layer. WHY: every side-store is PROPERTY-SHAPED RECORDS; carton lacked an exposed property surface + schemas + non-blob field updates -> each need spawned a side-store = the accumulated cost of the missing parameter layer. UNIFIED MODEL (grounded): triples=structure + properties=updating state (SET/WHERE) + schema-at-daemon-write-gate=invariants (ONE todolist per for_type = uniqueness; items must have status) + JIT=free navigation + carton's UNIQUE win = nodes ABOUT structures (discourse + structure in one graph; sqlite cards can't be talked about). ADOPTION TEST: new structures stop being exiled to side-stores; existing exiles become migratable. Worked example journaled: Bug_Todolist {for_type:Bug} HAS_ITEM items {status,priority,order}. Enabling build unchanged: property tools + daemon property support + schema validation at batch_create + JIT enrichment. (Also: sancrev's only carton WRITES from shell layer = boot-skill generator Mcp_Skill_* queueing in mcp_server:47-125 + skill_manager ontology; no carton-node-builder exists in sancrev either — closes the add_carton_node question from active code, having now read all of it.)  tags:[sancrev-read-complete, todolist, side-stores, unification]
