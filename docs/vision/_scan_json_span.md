# vision: Spec_Correction (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-09T21:30:52  FINDING: SPEC CORRECTION (supersedes the Build_Plan's 'find JSON end via json.JSONDecoder().raw_decode' instruction — that was MY error; the builder correctly deviated and flagged it instead of letting it pass silently). WHAT IS CORRECT: the fence body is JSON-WITH-BARE-REFS, so raw_decode throws on the first bare Title_Underscore token; and strictify-then-raw_decode returns an end offset in the STRICTIFIED string whose length differs from the original, so it does NOT map back to the ORIGINAL body offsets that the byte-identical splice (replace_carton_obj_body) REQUIRES. THE CORRECT APPROACH (implemented + verified): a hand-rolled string-context-aware depth scanner scan_json_span that walks the ORIGINAL text (skips JSON strings + backslash escapes, counts only real braces/brackets) -> exact ORIGINAL body span -> enables the byte-identical splice; THEN refs->strict-JSON + json.loads only to produce the parsed object. Same INTENT as the spec (real structural scan, NOT regex .*?; literal </CartonObj> inside a string harmless — proven by test_scan_json_span_close_fence_inside_string_ignored). Build_Plan amended: use the depth scanner, NOT raw_decode. Good catch by the builder; the verify-loop worked (the spec was wrong, the builder caught it, I confirmed from code I'd already read). Step 1 STANDS.  tags:[Spec_Correction, Scan_Json_Span, Raw_Decode_Rejected]
