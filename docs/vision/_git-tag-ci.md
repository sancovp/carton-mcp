# vision: pv2-0 (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T03:12:59  INTENT: DRIVING PV2-0 (#52) now — carton-kv build (the means) is DONE; this is the actual goal. The 3 carton-kv 'deferred' items (mirror-vs-replace, refs, test-cleanup) are NOT related to the goal — DROPPED. PV2-0 deliverable: kill the /tmp dev-clone divergence by auto-projecting public product repos from the monorepo via git-tag CI (heaven-bml-v2 tier1->tier2), codenose = worked example (public sancovp/codenose = BASE-only, monorepo automation/codenose = base + gnosys CA overlay; projection must EXCLUDE the CA overlay, keep it monorepo-side). Existing machinery (PUB-1..6 DONE): scalable-publishing/bin/publish_unit.sh (git-archive-HEAD + rsync + protect-list), publish-manifest.json (6 units, codenose NOT yet in it), heaven-bml-v2 tag public-* -> clone -> rsync -> push. codenose state: monorepo canonical 318f27c == runtime; /tmp/codenose d3e0b00 = hand-management to be superseded (local-only). PLAN: dispatch investigation -> map current infra + exact gap (the base-vs-CA exclusion mechanism + wiring codenose) -> dispatch build -> verify projection produces clean base-only codenose LOCALLY (no public push yet; public push is a separate gated outward action). Commander dispatches; does not freestyle.  tags:[pv2-0, codenose, git-tag-ci, divergence-kill]
