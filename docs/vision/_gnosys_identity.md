# vision: Gnosys_Identity (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-23T04:08:06  FINDING: ROOT CAUSE of the 'gnosys has 0 :Wiki nodes' Core-gate finding = a NAMING DECOHERENCE between the SM-gate actor key and the identity graph representation, NOT a missing identity. MECHANISM (verified): the SM gate's actor = get_active_identity() = the LITERAL contents of /tmp/heaven_data/carton_sm_active_identity = the lowercase string 'gnosys' (written by equip_persona, Isaac 2026-06-20 'equipping a persona makes its carton_identity the gate's actor'). _lock_into_sm_chain does MATCH (a:Wiki {n:'gnosys'}) — but NO node is named lowercase 'gnosys'; graph names are Title_Cased. The identity IS represented, REDUNDANTLY / mid-transition: (1) Gnosys_Collection is_a Identity_Collection (is_a Carton_Collection) = the OLD collection way (observe_from_identity_pov); (2) Gnosys_Identity is_a Agent_Identity (Agent_Identity is_a Carton_Ontology_Entity+Doc_Mirror_Domain) = the NEWER 'actual thing' way; (3) Gnosys = the doc-mirror domain / Biographical_Entity facet. The actual-thing/frames side is PARTIALLY STUBBED: GNO_SYS_Identity, Gnosys_Identity_State, Relational_Identity_Pattern are all AUTO-CREATED stubs (no description). TIMELINE corroboration (Isaac's memory is correct on BOTH): June 17-20 there WAS an observation/identity/collection AUDIT + frames clarification (Iteration_Summary 2026-06-17T06:45 'audit the observation/identity/collection', 06-17T07:01 'frame = role prompt', 06-20T13:11 'frames were supposed to BE personas but diverged'). So identities WERE collections AND there was a move to make them actual-things-with-frames (frame=role-prompt=persona) — but it's mid-transition with stubs. VERDICT: NOT a missing-thing (don't make a new identity rep). It IS a small, reconnect-gated reconciliation problem: the SM gate keys on the raw lowercase persona handle but was never reconciled to the identity ENTITY node. The clean fix ALIGNS with the actual-things-with-frames change: resolve the gate's actor to the identity entity node (Gnosys_Identity) so the Execution_State/lifecycle (the 'frame'/state) attaches to the actual identity. This is a DESIGN decision in the identity/persona/gate layer (coupled to finishing the actual-things-with-frames model + canonicalizing Gnosys_Identity / killing the stubs) = Isaac-owned / reconnect-era, NOT auto-build. CARTON_SM_GATE is default-OFF so nothing live is broken now.  tags:[Gnosys_Identity, Agent_Identity, Identity_Collection, Sm_Gate, Frames]
