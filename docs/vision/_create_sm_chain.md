# vision: carton-mcp (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-21T02:19:47  INTENT: Build create_sm_chain in sm_gate.py — the first-class SM-creation factory abstracting the proven test_sm_core_e2e _program cypher (concept-HAS_SM_CHAIN->Sm_Chain-SM_CHAIN_RUNS{order}->State_Machine-HAS_STEP->Traversal_Step{required_pattern,text}, NEXT_STEP between consecutive steps). Idempotent MERGE-on-names like skill_to_sm; uses module type constants; gated=len(state_machines)>1. New test_sm_factory_e2e.py modeled on the core test, self-cleaning Zztest_Factory_ prefix.  tags:[carton-mcp, sm_gate, create_sm_chain]
- [v2]  2026-06-21T02:21:12  FINDING: create_sm_chain built + E2E GREEN against live neo4j (3/3 PASS, exit 0): structure (HAS_SM_CHAIN/SM_CHAIN_RUNS{order}/HAS_STEP/Traversal_Step{required_pattern} queried back; order 0=show,1=gate), gating (factory-built 2-SM stack arms require_next + locks at gating step via sm_chain_visit), idempotency (double-call same counts). pip install --no-deps confirmed; installed package has create_sm_chain. Did NOT change any existing function. Not yet wired onto live MCP surface (out of scope).  tags:[carton-mcp, sm_gate, create_sm_chain]
