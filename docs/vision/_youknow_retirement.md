# vision: Youknow_Retirement (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-21T04:15:10  FINDING: SCOPED Claude task #18 ('remove dragonbones validate_system_type + remaining youknow consumers') against the REAL code before touching — it is NOT a clean dead-code removal safe for AFK. (1) validate_system_type is NOT in the dragonbones package at all (files: compiler/constants/giint_types/logs/main/mcp_tools/parser/server/transcript). It was the CARTON-side system_type_validator, already removed in task #16. So that half is MOOT/done. (2) The real youknow remnants in dragonbones: (a) the hide_youknow PARAMETER NAME in compiler.py:554-562 + logs.py:104-178 -- this is a STALE NAME for a LIVE gate (it gates whether the SOMA :8091 call fires; carton add_concept_tool.py:2000 reads the SAME param 'if SOMA_AVAILABLE and not hide_youknow'). Renaming it = a CROSS-BOUNDARY dragonbones<->carton coordinated rename of a LIVE contract, NOT a removal -- touches carton internals = the AFK 'dont touch carton/dragonbones unplanned' constraint. (b) the '"YOUKNOW error"' substring match at compiler.py:566 (silent-mode SOUP surfacing) -- the dragonbones reference docs themselves flag it UNVERIFIED whether SOMA emits that substring; fixing it correctly needs SOMA's REAL error-string = live-surface verification. (c) reference/ docs already correctly marked STALE (D2/D3/D4/D6 diagrams + 08-soma-handoff). VERDICT: #18 as written is partly moot + the rest is a live cross-boundary rename + a SOMA-output-dependent fix = needs Isaac/planning + live SOMA, NOT AFK-safe. Re-scoping the task; NOT doing it AFK.  tags:[Youknow_Retirement, Soma_Migration]
- [v2]  2026-06-21T04:57:34  FINDING: #18 DONE + VERIFIED + COMMITTED (fork-on-change, 4 commits 9ae7b4e+3). Isaac greenlit ('why do u need me for 18 - do it') -- I'd over-flagged it. THE TRUTH (read complete footprint): dragonbones has ZERO youknow CONSUMERS (no import, no call; only youknow( match is a dead-code comment). validate_system_type is NOT in dragonbones (was carton system_type_validator, removed #16) = moot. Validation has been carton-side SOMA since 2c13f0b. So #18 = retire the youknow RESIDUE: (1) removed the ONE dead behavior item: 'YOUKNOW error' substring at compiler.py:608 (SOMA emits status=:soup/soup_gaps=, NEVER 'YOUKNOW error' -- confirmed from add_concept_tool; SOUP branch intact). (2) De-staled all validator-YOUKNOW comments -> SOMA across compiler.py/constants.py/parser.py/giint_types.py (state-what-is). (3) DELIBERATELY LEFT the live hide_youknow API field (logs.py x3 + compiler.py:602) -- it is woven through carton add_concept_tool.py incl. the observation_data MCP-contract key (:1588); renaming = breaking the carton MCP API for a COSMETIC stale name = wrong engineering call (clarify-with-comment beats break-a-live-API; it is a gate flag, NOT a youknow consumer). VERIFIED: test_sm_ec_e2e 5/5 PASS (the compile path I touched) + clean imports; logs/transcript partial-passes are PRE-EXISTING (git diff confirms I touched only 4 files, NOT logs.py/transcript.py). Fork-on-change: re-derived all 4 doc(m) (YOUKNOW->SOMA, preserved hide_youknow) via doc-mirror-commit. Closed Claude task #18. NOTE: the hide_youknow->hide_validation rename remains as a SEPARATE future cross-boundary refactor (carton MCP contract) if ever wanted -- not part of youknow-CONSUMER removal.  tags:[Youknow_Retirement, Soma_Migration]
