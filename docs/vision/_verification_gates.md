# vision: Carton_Obj (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-09T21:16:19  INTENT: BUILD-EXECUTION CONTRACT for the CartON KV subsystem (read WITH the Locked_Spec cohered note — docmirror-read state Locked_Spec — which holds the full design; this entry is the ORDERED BUILD + how to verify). CANONICAL SOURCE = monorepo /home/GOD/gnosys-plugin-v2/knowledge/carton-mcp/ (NEVER the /home/GOD/carton_mcp dev clone, NEVER site-packages). RUNTIME = pip install --no-deps the monorepo carton-mcp then reconnect_mcp carton (NEVER pkill an MCP). ONION ARCH (mandatory): every capability is a carton_utils LIBRARY function FIRST (unit-tested at lib level), then a thin server_fastmcp MCP TOOL wraps it. EXPANSION CLARIFIED (Isaac): ref-expansion is a READ-TIME PROJECTION at the LIBRARY level, applied AFTER the retrieval query returns — it replaces the bare Title_Underscore ref token in the RETURNED output with that concept's description + graph (recursive to depth N, cycle-guarded); the STORED n.d is never mutated by expansion. DISCIPLINE: read-whole-file / complete-operational-boundary before any edit; check CODE not markdown; state IS-vs-VISION; build ENDS in a dev-flow skill (edit-carton-kv) + a use-rule (every-build-ends-in-a-development-flow-skill). ===ORDERED PIECES (each: lib fn + unit test -> report -> COMMANDER verifies E2E through the real MCP tool after pip-install+reconnect -> only then next):=== STEP 0 GROUNDING READ: trace + write a boundary map (to /tmp/heaven_data/publishing_inventory/carton_kv_boundary.md) of: where the auto-linker lives (the write-path that rewrites Title_Case mentions into wiki-links), the n.d WRITE path (add_concept/observation worker), get_concept (knowledge/carton-mcp), the wiki-link STRIP location (carton_utils strip_wiki_links / _execute_neo4j_query — added recently), and add_concept's RELATIONSHIP auto-stub creation (the code that creates a stub target node for an unresolved related concept — the validation guard must hook here). NO edits in step 0. STEP 1 PARSER/NORMALIZER (carton_utils): find <CartonObj name=..> fences in a string by JSON raw_decode span (NOT regex .*?); convert bare Title_Underscore values <-> {\"$ref\":\"Name\"} <-> strict JSON; parse + re-serialize + splice back by offsets (literal close-fence inside a JSON string must be harmless). Unit-test all round-trips incl edge cases (multiple flat fences, nested JSON, refs in arrays, literal-quoted strings, a literal </CartonObj> inside a string value). STEP 2 edit_carton_obj(concept,kvobj_name,key_path,op{set|append|remove|get},value): read n.d -> locate named fence -> parse -> apply op at dotted/indexed key_path -> reserialize -> splice -> write n.d; leaves prose + other fences byte-identical. MCP tool wraps it. E2E gate = call the MCP tool, edit one leaf, confirm only that leaf changed. STEP 3 LINKER FENCE-OPACITY: teach the write-path auto-linker to SKIP <CartonObj>..</CartonObj> regions (so refs inside are never rewritten into _itself.md links). E2E gate = write a concept with a CartonObj full of Title_Underscore refs, confirm n.d stores them RAW (no md links injected). STEP 4 SCHEMA REGISTRY (dogfood): an is_schema=true fence (json-schema body) on write auto-types its concept IS_A Carton_Kv_Schema (SOUP graph + edges what-for/how/used-by); validate(concept,kvobj) resolves the schema=Concept attr -> json-schema validate -> on fail report bad key+error; on any bare ref that is NOT an existing concept, return FUZZY nearest-matches (did-you-mean) and REFUSE (never auto-create a stub). E2E gate = define a schema concept, reference it, validate a good + a bad payload, confirm the did-you-mean on an unknown ref. STEP 5 REF-EXPANSION in get_concept(name, expand_refs=True, depth=N): library-level read-time projection; replace each bare ref with target description+graph recursive to N, cycle-guarded; depth=0 default = unchanged behavior. E2E gate = get_concept with expand_refs at depth 0/1/2 + a deliberate cycle. STEP 6 DEV-FLOW SKILL edit-carton-kv + use-rule (coherence set: carton_utils lib <-> server_fastmcp MCP <-> linker <-> get_concept <-> schema-graph; + deploy = pip --no-deps + reconnect; + the E2E test). STEP 7 FIRST USE: build the publishing unit-registry as a CartonObj (returns to the publishing backbone). VERIFICATION SPLIT: teammate builds + unit-tests at LIBRARY level per piece; COMMANDER (me) does the MCP-surface E2E gate (verify-via-user-surface + verify-agent-results-yourself) before the teammate advances. NAME = CartonObj (confirmed).  tags:[Carton_Obj, Build_Contract, Onion_Arch, Dev_Flow, Verification_Gates]
