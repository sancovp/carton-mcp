# vision: Carton_Obj (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-09T21:13:46  DECISION: LOCKED SPEC (Isaac): the CartON KV subsystem = a self-hosting KV capability inside carton descriptions. SYNTAX: <CartonObj name=X schema=Some_Schema_Concept>{ JSON-with-bare-refs }</CartonObj> embedded in a concept's description (n.d); multiple flat (NON-nested) fences per concept; depth lives in the JSON, not nested fences. REFS (python-dict-style): a BARE Title_Underscore token as a value = a carton concept ref ('variable'); a QUOTED string = literal data — quoting disambiguates (no auto-link guessing). NORMALIZER: converter turns bare refs into ref-tags -> strict JSON -> validate vs schema -> on failure report bad key + error; round-trips on write (splice by json raw_decode offsets so a literal close-fence inside a string is harmless — NO forbidden-literal rule needed). LINKER: teach the auto-linker (write path) to treat the fences as OPAQUE (skip) — the linker itself is CORRECT (Isaac: the 36.5k stub pollution is an UPSTREAM TRUNCATION bug feeding it half-fragments, a SEPARATE problem). WIKI LINKS move from write-time-pollution to PUBLISH-TIME PROJECTION (ref -> markdown wiki link only when emitting the github wiki). SCHEMA REGISTRY = DOGFOODED ON CARTON: define a schema AS a fence (is_schema=true, json-schema body) on a concept; writing it auto-types the concept IS_A Carton_Kv_Schema (SOUP-layer graph, NOT SOMA pipeline) with edges (what-for/how/used-by) = browsable schema GRAPH; other fences reference it via schema=attr; renderable in get_concept. REF-EXPANSION: get_concept(name, expand_refs=True, depth=N) REPLACES each bare ref token IN THE RETURNED OUTPUT with the referenced concept's description + graph, recursive to depth N, cycle-guarded (heaven SimpleRegistryService cycle-detected pointer-resolution = precedent); depth=0 default (raw tokens, unchanged); STORED form keeps the ref (replace is render-only). LOCKED MICRO-DECISIONS: (1) expand=REPLACE-on-render (Isaac); (2) schema=Concept_Name attribute on the fence; (3) is_schema=true attribute drives auto-typing; (4) validate on every edit + FUZZY did-you-mean on unresolved refs = the CURE for the auto-stub disease (refs must resolve or nearest-matches, never silently spawn a stub). TOOLS (Isaac's usage): edit_carton_obj(concept,kvobj,key_path,op{set|append|remove|get},value) + get_concept(expand_refs,depth) + register/validate. ONION ARCH: carton_utils library fns first, MCP tools wrap. KILLS 3 birds: tracking + stub-pollution + link-pollution. FIRST USE = publishing unit-registry as a CartonObj. NAME TO CONFIRM: CartonObj (vs earlier CartonKvObj).  tags:[Carton_Obj, Kv_Edit, Schema_Registry_Dogfood, Ref_Expansion, Publishing_Registry]
