# vision: map02 (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T11:46:10  FINDING: MAPS 02/03/05 landed (drafts at /tmp/heaven_data/carton_full_map/). LOAD-BEARING FACTS (per mappers, my spot-checks pending): [TREESHELL map02] JIT-from-carton EXISTS + LIVE: _jit_node_from_carton (heaven-tree-repl base.py:1240-1317) fires on jump/menu miss, queries c.n + rels, synthesizes Menu node, caches; children come from HAS_OPTION ONLY (base.py:1283-1291 — HAS_PART/IS_A/etc fetched then DISCARDED); node_sync round-trips 5 fields via NODE_DEF comment but only 'options' is consumed (learnings.py:57-77, base.py:1329-30). THE BIJECTION GAP, enumerated: the entire Callable type (function_name, import_path, import_object, function_code, is_async, args_schema, default_args, signature) + option labels/order + family topology + nav_config + zones + shortcuts — JSON-only => carton can NEVER yield an EXECUTABLE node today, only menus. [SOMA map03] SOMA daemon :8091 DOWN; SOMA_AVAILABLE frozen at MCP import (add_concept_tool.py:59-76, gate :2037) => NO validation fires on ANY carton write right now; carton stores UNCONDITIONALLY = true SOUP store; validation (when live) never blocks anyway — only REQUIRES_EVOLUTION edges + SYSTEM_TYPE projection gating; youknow re-promotion hard-disabled (daemon :365-367 return 0); vault._mirror_to_carton = the ONLY SOMA->carton write; SOMA never reads neo4j (own SQLite quadstore). [ODYSSEY/AUTOBIO map05] Odyssey = 2 cave organs + 9 SDNAC jobs, Episode/Scene/Dialog/Journey/Epic model — PARKED since ~Apr 19-24, starved on L5 Executive_Summary (L5/L6 backlogged since Mar 03 per map04, even though L1/L2 are LIVE). Autobiographer = SUBSTANTIALLY IMPLEMENTED (JournalAgent + AutobiographerNight + sanctuary MCP journal_entry/deposit_memory; Journal_Entry newest 2026-06-06). AWAITING: map01 carton-core + map06 giint, then carton-mcp init population.  tags:[map02, map03, map05, treeshell-jit, soma-down, bijection-gap]
