# vision: carton-kv (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T01:42:34  FINDING: STEP 5 GATE PASSED — commander-verified E2E myself through the real mcp__carton__get_concept MCP surface (not a report). All 4 checks pass on the pre-made test concepts: (1) get_concept(Kv_Exp_Top, expand_refs=False, depth=0) = RAW, bare Kv_Exp_Mid, no MIDMARK; (2) depth=1 = MIDMARK present, LEAFMARK ABSENT (one hop only); (3) depth=2 = LEAFMARK present (recurses 2 levels); (4) get_concept(Kv_Exp_A, expand_refs=True, depth=3) over the A<->B cycle = expands A->B->A->B then TERMINATES with an explicit '…cycle⟩' guard marker, NO HANG. Expansion is render-only (stored n.d unchanged), wraps target as ref⟨desc ‖ rels⟩, cycle-guarded per Locked_Spec. Steps 1-5 of the carton-kv build now ALL DONE + commander-gated E2E. NEXT = Step 6 (edit-carton-kv dev-flow skill + use-rule) then Step 7 (publishing unit-registry as the first real CartonObj). Deferred: clean test pollution (Kv_Exp_*, Carton_Kv_* test concepts, stubs Test_Concept/Test_Pattern/Cave_Repo/A_Unit/B_Unit) after Step 6/7 land.  tags:[carton-kv, step5, gate-passed, ref-expansion]
- [v2]  2026-06-10T16:33:44  FINDING: PHASE 1 DEPLOYED + E2E GATE PASSED (commander, real MCP surface): builder commit 2d23406 (set_concept_properties/query_concepts_by_properties/remove_concept_relationship lib + 3 thin tools + Props render in get_concept; 20/20 new tests + all 4 existing suites green); pip --no-deps + reconnect; GATE: set_properties on Publishing_Unit_Registry set {status:mirror, unit_count:6} + REFUSED reserved d/linked; query_by_properties found it by fact with IMMEDIATE read-after-write (sync path confirmed); get_concept renders Props:{...} between Description and Rels; fence + n.d byte-untouched. THE PROPERTY LAYER IS LIVE. METASTACK ANALYSIS (read 100%: core 222 + __init__ 192 + fractal 159 + mcp_server 377): RenderablePiece (pydantic + render()->str, NESTED typed composition) + MetaStack container + FractalPattern + registry (register by name w/ class_path/domain/tags/defaults; render_metastack(content_dict); render_to_file). ISAAC'S MAPPING CONFIRMED ~90% BUILT: (1) properties=template-KVs: render consumes a content dict, properties ARE one; substrate_projector.render_through_template (:1043-1129) ALREADY builds content from concept data + renders through registered template before projecting — predates properties, so ONE EXTENSION: merge properties(c) into its template_content. (2) labels<->metastack = UNIFICATION: a metastack class IS pydantic-schema+renderer => Label_Schema rung CAN BE the metastack class (validation+rendering one object). (3) property->property templating possible but prefer render-at-projection (redundancy principle); (4) 'become n.d when projected to file' = exactly right materialization: node keeps gloss+props, FILE content = the render. NEW PIECE WORTH BUILDING: graph->template HYDRATOR (~60-80 lines: scalars from properties, nested RenderablePiece fields by following refs/edges to child concepts hydrated through THEIR templates) => whole graph subtrees render as one typed document (typed transclusion; FractalPattern from graph children). KILLER EXAMPLE: publish-manifest.json becomes a PROJECTION of Publishing_Unit_Registry + unit property-nodes through a publish_manifest template => mirror-vs-replace dissolves (carton=truth, JSON=render; README/marketplace/aisaac-feed = other templates over same nodes) = publishing's final shape: graph->templates->world. CAVEATS: template registry = heaven file RegistryService (self-marked 'PARALLEL: should migrate to CartON'; templates-as-concepts later); template LOGIC stays python (graph holds content, code holds form). DOC-DEBT: doc(m) re-derivation queued for server_fastmcp.py + carton_utils.py (builder noted get_concept doc now stale-by-omission).  tags:[phase1-live, gate-passed, metastack-bridge]
