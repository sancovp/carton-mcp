# vision: knowledge/carton-mcp/chroma_daemon.py (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-28T18:45:36  FINDING: FINDING: task-41 (rip every chroma import to thin daemon HTTP calls) is VERIFIED BUILT/DONE - the task label decided-never-built is STALE (check-code-not-markdown). LIVE CODE-VERIFIED 2026-06-28: the ONLY files in carton-mcp importing chromadb/langchain/onnxruntime are chroma_daemon.py (THE sanctioned sole importer, the daemon process) and smart_chroma_rag.py (the daemon ENGINE it uses); a clean grep over the whole package source (excluding build/pycache/tests) shows zero other importers. Empirically confirmed on the INSTALLED running copy: importing carton_mcp.carton_utils + carton_mcp.add_concept_tool pulls in ZERO chromadb/onnxruntime/langchain modules (sys.modules diff = NONE). Every other consumer (enforce_ontology_invariants, etc.) goes through the thin urllib chroma_client over HTTP. Matches canonical-source-dirs (carton-mcp is now FULLY chroma-free + langchain-free; the daemon pattern built+verified 2026-06-25). This was ALSO the incidental fix that removed onnx/chroma from the query path (relevant to task-54: the print-clobber leak is heaven_base, NOT chroma, confirming chroma is fully out of carton's lib path). Closing task-41. No backticks.  tags:[knowledge/carton-mcp/chroma_daemon.py]
