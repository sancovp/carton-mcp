# vision: Four_Lenses (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-21T01:31:03  DECISION: THE WHOLE VIEW (Isaac 2026-06-21, his design synthesis -- key phrases near-verbatim). Every dir is supposed to become a STARSYSTEM, which lets you call STARSHIP PILOTS on it. Plan the rest of the agents FROM THIS UNDERSTANDING. Without changing really anything, each starship pilot is automatically the CODER/EVOLVER AIOS fork of a domain/entity thing being programmed from carton. We will have to FORK the domain/entity, because: the coder/evolver view vs the domain view vs the agent-persona view (that is running and being evolved) -- definitely have to be DIFFERENT THINGS. THE THREE VIEWS (= the FOUR LENSES from the carton shape, mapped): (1) DOMAIN view = the Domain lens = just WEBBING THE KNOWLEDGE, POPULATING FACTS (== the Steward role: fill+web a domain with facts; not workflows, not code). (2) AGENT-persona view = the Agent lens = DOING ACTUAL WORKFLOWS; the persona that is running and being evolved; THE AGENT IS ALSO IN THE PLACE/WORLD (the Place lens = the agent's world-context). (3) CODER/EVOLVER view = the Code-Object lens = EVOLVING THE CODE THAT IS THE AGENT = THE STARSHIP PILOT. So a domain/entity forks into 3 distinct lens-agents (Domain-webber / Agent-runner-in-Place / Code-Object-evolver=pilot) because they do 3 distinct jobs. THE MECHANISM (Isaac): we DON'T need to change starsystem; we need to INCORPORATE it / TIE IN the way it works TO CARTON, so it AUTOMATICALLY MAKES THIS STUFF -- which PROBABLY MEANS SOMA D-CHAINS (observe a dir-as-starsystem -> d-chain fires -> instantiates its lens-views/forks + the pilots; same shape as giint completeness d-chains). === IS vs VISION: IS = the 4 lenses exist in carton (is_a edges); starsystem geometry welded 2026-06-11; starship-pilot = a Ghost/persona per starsystem. VISION (build) = (a) fork a domain/entity into its 3 lens-agents as DISTINCT things; (b) make it AUTOMATIC via SOMA d-chains tying starsystem-way-of-working into carton; DO NOT rebuild starsystem. === ARC ALIGNMENT: #2 dragonbones+ECs-drive-state-machines = the AGENT lens workflows; #3 'make starsystems active' = the d-chains that auto-fork a dir into Domain/Agent/Evolver + spawn pilots. NEXT (Isaac): plan the rest of the agents from this understanding = spec the 3 lens-agents + the auto-fork d-chain. (Prescan corroboration to check: Idea_Compiler_Agent_Landscape_And_Starsystem_Unification.)  tags:[Four_Lenses, Starship_Pilot, Soma_Dchains, Coder_Evolver]
