# vision: carton (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-07-04T04:18:55  FINDING: FINDING: Task 58 done, per Isaacs direct scoping (2026-07-04, verbatim): it can move into add concept tool func but as optionals because it cant change anything about how other code uses carton as lib unless we wanna go thru literally all the code and check for these calls and adjust them. add_concept_tool_func gained four new OPTIONAL params (domain, subdomain, personal_domain, produces), append-only after every existing param since callers use it positionally, so all ~8 existing bypass callers (Dragonbones compiler.py, sm_gate.py, split_content_concept, migration scripts) remain completely unaffected. Read add_concept_tool.py in full around the relevant region and server_fastmcp.pys add_concept MCP tool (lines 470-593) to mirror its has_domain/has_subdomain/has_personal_domain/produces convenience-building exactly, minus the requirement. New pure helper merge_optional_domain_fields, unit tested (7 green, test_concept_provenance_optional_fields.py). Caught a real bug myself during E2E verification, not just trusting green unit tests: the first draft merged the new fields into relationship_dict only -- that validates correctly via SOMA and D2 (a plausible-looking D2 coverage message even appears, listing the fields as declared-but-unmatched) but relationship_dict is a DERIVED view used only for SOMA/D2 validation -- the actual daemon queue write persists the ORIGINAL relationships list verbatim (queue_data relationships = relationships), so the fields never reached the graph at all. Only re-querying the live graph after the call, not the green tests or the plausible D2 message, surfaced this. Fixed by operating on the relationships list itself and reassigning it before relationship_dict is built. Verified live twice after the fix: a direct add_concept_tool_func call with all four fields lands real HAS_DOMAIN/HAS_SUBDOMAIN/HAS_PERSONAL_DOMAIN/PRODUCES edges (personal_domain normalized to Cave title-case per the naming convention); a zero-kwarg old-style call, matching every existing bypass caller exactly, still succeeds identically to before this change. Re-ran test_description_rollup/test_d2_coverage/test_split_content/test_relationship_constraints -- all green, no regressions. Wrote the dev-flow skill (edit-add-concept-optional-fields) plus repo rule per every-build-ends-in-a-development-flow-skill, explicitly documenting the list-vs-dict bug so it cannot be silently reintroduced. Committed+pushed as f0b5e60 (also force-added the same recurring .claude gitignore gap a third time this session). NEXT: genuinely still open, unchanged -- this does NOT migrate any of the ~8 bypass call sites to actually pass the new fields; that audit-and-fix pass is separate future work, not yet scoped or actioned, per Isaacs own framing that going through all the calling code is out of scope right now.  tags:[carton, add-concept, task-58]
- [v2]  2026-07-04T04:48:58  FINDING: FINDING: Task 58 follow-through -- sm_gate.py and dragonbones now REQUIRE domain/subdomain/personal_domain for the SM abstraction, per Isaacs direct reconsideration: is that work actually out of scope? ... Dragonbones should definitely require it, sm_gate should require it for the abstraction about the sm... see? then also necessitates changes to dragonbones again. Unlike add_concept_tool_func (kept optional, ~8 existing bypass callers across the monorepo), create_sm_chain/create_sm_chain_live are this sessions own new capability with exactly 4 known callers, all updated together, so requiring them carries none of that risk. Before touching code, pulled Pattern_Dragonbones_Is_Only_Syntax_Layer (a 2026-05-12 decision that Dragonbones must never be a shape/type registry in Python) and reconciled it: that pattern targets ontological TYPE-SHAPE dicts (GIINT_EC_SHAPES etc), a different category from administrative provenance-field requiring, which already has precedent at the MCP-tool layer (add_concept requires these same 4 fields, accepted without objection) -- proceeded on that basis rather than asking, per the recent-conversation-first deconfabulation discipline. Read compiler.py, logs.py, db_parser_v2.py, db_carton.py, node_sync.py, substrate_projector.py, and server_fastmcp.py in full before editing. Key finding: the EC notation ALREADY supports arbitrary key-EQ-value customs (has_domain/has_subdomain/has_personal_domain need zero parser changes, authored exactly like the existing gates custom). Implemented: sm_gate.create_sm_chain/create_sm_chain_live now raise if domain/subdomain/personal_domain missing or personal_domain fails PERSONAL_DOMAINS validation; tags the Sm_Chain node (not every State_Machine/Traversal_Step, which would be redundant) with HAS_DOMAIN/HAS_SUBDOMAIN/HAS_PERSONAL_DOMAIN plus PRODUCES if given. Updated all 4 production call sites (substrate_projector.project_state_machine reads the SM concepts own has_domain/has_subdomain/has_personal_domain and passes through, refusing cleanly with a specific message if missing; db_carton.py and node_sync.py _sm_graph_to_factory, both copies in lockstep, read the same fields off the parsed graphs customs dict; server_fastmcp.create_branching_sm gained the same required params) plus 10 test files (7 direct call-site updates across 6 files, 3 files that manually seed neo4j for project_state_machine or the typed router). Caught and fixed a REAL bug during E2E verification, not just green unit tests: project_state_machine reads personal_domain back off the graph as a Title_Cased node name (Cave, since the daemon Title-Cases every relationship target it writes), but create_sm_chain enum-validated case-sensitively against the lowercase PERSONAL_DOMAINS list, so this exact round-trip raised -- caught by test_project_state_machine_branching_e2e.py, which I ran live, not assumed passing. Fixed by validating case-insensitively (both the raw lowercase form and the Title_Cased round-trip form are legitimate) and Title-Casing for the write regardless. Re-verified green after the fix: test_sm_branching.py 19/19 unit, plus the full live E2E regression set across carton-mcp, dragonbones, and heaven-tree-repl, all green. Also ran a fresh direct create_sm_chain_live call confirming missing-fields raises, invalid personal_domain raises naming the valid enum, and a full valid call lands all four relationship types on the Sm_Chain (verified via query_wiki_graph, not just a success return). Updated the dragonbones-carton-retrieval-state-machines dev-flow skill with a full Step 5 section matching its established per-step style, plus updated all three authoring-path examples (dragonbones EC, treeshell inline, create_branching_sm) to show the new required fields. Committed+pushed as a9555c0 across all three repos in one commit (monorepo). NEXT: explicitly flagged, NOT actioned -- the GENERAL requirement across every Dragonbones-compiled concept (not just State_Machines), i.e. compiler.pys two general add_concept call sites plus logs.pys CogLog/SkillLog/DeliverableLog, is a much larger system-wide change (Dragonbones is the unified output format for every turn) with a fundamentally different risk profile than this SM-specific change. Recommending Isaac decide explicitly whether/how to scope that before it gets forced through, rather than doing it silently in this same pass.  tags:[carton, sm-gate, dragonbones, task-58]
