# vision: correction (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T12:39:34  FINDING: CORRECTION to the as-is catalog (Isaac challenged 'callables not stored on carton' — he was partially right; I read node_sync.py 165 lines + grepped all engines + /core myself). PRECISE TRUTH: (1) treeshell->carton: node_sync.py DOES store every rendered node as {App}_Node_{Semantic_Name} with NODE_DEF={type, options, signature, args_schema, domain} (:97-104) + is_a TreeShell_Node + collection/version/domain rels — so the node INTERFACE (incl args_schema + signature) IS on carton; ONLY the import binding (function_name/import_path/import_object/is_async/function_code) is absent from NODE_DEF (grep-verified). (2) carton->treeshell: JIT consumes options only (learnings.py parse, base.py:1329-30); callable processing (base.py:254-264,400-410) runs on family-JSON node_data only. (3) ISAAC'S MEMORY = the Add_Carton_Node_And_Sync_Functions concept (Oct2025+Jan2026 observations): add_carton_node imported code 'if callable/awaitable ref present', sync_with_carton batch-added should_be_treeshell:true concepts — grep across current engine + sancrev-treeshell + skill-manager + legacy /core + whole disk = ZERO implementations; survives ONLY as the carton concept = a THIRD D2-pattern ghost (after _compute_description_rollup and the stale ingestion-dead note): capability built-or-designed earlier, absent from running code, remembered as present. IMPLICATION: properties design = RE-IMPLEMENTING add_carton_node properly; executable gap shrinks to FOUR fields (function_name/import_path/import_object/is_async) + the JIT dict-copy; args_schema/signature/options/type already sync. The Properties_Design vision entry stands, with this narrower gap.  tags:[correction, node-sync, add-carton-node-ghost]
