# vision: Carton_Power_Chain (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T17:44:06  DECISION: STEP 2 (Small_Fixes) DONE + pushed (4c00e54..f8d8ee3), verified MYSELF (read the hunks in the files + re-ran test_carton_properties.py 21/21 incl. the new merge test): cd37c66 substrate_projector render_through_template merges non-reserved node properties via new pure helper _build_template_content (props fill missing keys, explicit concept-data wins, RESERVED_PROPERTY_KEYS imported); 544c1ae chroma cache fix (heaven_data_dir defined locally — CORRECTED understanding: was a SWALLOWED warning that silently never wrote the cache, breaking query_graph_from_rag_result, not a tool crash); f8d8ee3 edit-carton-kv SKILL.md now states ONE live parse path positively. Both doc(m)s re-derived through doc-mirror-commit --doc-from (the new lane, working as designed). Chain: Small_Fixes done, current_order=3. FOLLOW-UPS FLAGGED by builder (queued, not done): (a) .claude/rules/editing-carton-kv-uses-the-dev-flow.md STILL asserts two-parse-paths + carton_utils.py:59 docstring routes through dead process_queue_file; (b) server_fastmcp doc(m) has file-wide line-ref drift (~100 lines, e.g. chroma_query :2575 vs actual :2683) — needs a full re-derive pass; (c) _build_template_content props-as-extra-kwargs UNVERIFIED against a registered template that rejects unexpected kwargs; (d) test_carton_properties.py has no doc(m) (closure gap, module postdates the 18-module population). pip --no-deps deployed; carton MCP server reconnect pending (new server code takes effect on next MCP reconnect/session).  tags:[Carton_Power_Chain, small-fixes]
