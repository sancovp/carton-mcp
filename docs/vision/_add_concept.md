# vision: Add_Concept (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-22T22:17:46  FINDING: BUILT+VERIFIED the KEYSTONE (commit 152f684): add_concept now carries node properties -> the daemon applies set_concept_properties. Isaac's 'big mistake' (add_concept had no properties channel) is fixed. WHY n.d-JSON existed: set_concept_properties MATCHes (node must exist) + a release-effect handler RACES async node creation -> STAGE C smuggled SM as <sm_spec> JSON in n.d. RESOLUTION: the DAEMON applies set_concept_properties during the SAME drain that MERGEs the node (node exists, in order, no race). EDITS (3 carton files): (1) add_concept_tool.py add_concept_tool_func gains properties param+docstring + queue_data carries 'properties'; (2) observation_worker_daemon.py parse_queue_file_to_concepts raw_concept branch forwards properties + batch_create_concepts_neo4j applies set_concept_properties per concept AFTER node MERGE (reserved-key refuse, scalar/flat-list validate, best-effort SOMA trail for ontology-bearing nodes, wrapped so a prop-failure never breaks the write, returns properties_set); (3) server_fastmcp.py MCP add_concept tool gains properties param+forwards -> add_concept is the one-call universal write (edges AND properties). TEST test_props_keystone.py PASS: queue.properties->parse->batch_create->set_concept_properties->neo4j node carries sm_gate_test='open'+order=3, reserved n refused (not clobbered), NO prod SOMA :8091 POST (untyped probe -> trail scratch-lane), DETACH DELETE cleanup. Per the-property-layer-doctrine scratch lane. Both add_concept_tool_func AND dragonbones db_carton are producers of the queue JSON; the daemon is the single consumer applying it. NEXT: SLICE 2 parsers parse 🏷->properties + carry to queue JSON.  tags:[Add_Concept, Set_Properties, Keystone, Daemon]
