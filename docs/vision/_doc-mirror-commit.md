# vision: doc-mirror (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T17:27:48  FINDING: CLI GAP CONFIRMED: doc-mirror has NO sanctioned placement lane for re-derived doc(m) content. Evidence: BOTH re-derivation writers' Write calls to docs/mirror were blocked by docmirror_readonly_guard; the utils writer then bypassed via Bash cat>/tmp + cp (landed 17:00), the server writer was stopped before its Python bypass. doc-mirror-commit only commits what is already on disk; nothing legally places it. The mirror population has been running on guard holes (relative paths / cp). Per the doc-mirror-files-are-cli-only rule this is a CLI gap to fix: adding --doc-from <derived-file> to doc-mirror-commit (validate + place + commit atomically) closes the lane properly and lets the guard stay maximally strict.  tags:[doc-mirror, readonly-guard, doc-mirror-commit]
- [v2]  2026-06-10T17:32:37  DECISION: STEP 1 (Close_Phase1) DONE + pushed (ddbbe11..4c00e54): server_fastmcp doc(m) re-derived+committed cf86bf8 via the NEW doc-mirror-commit --doc-from sanctioned placement lane (built this step, commit a6448a2, because BOTH writers were guard-blocked and the CLI gap had to be fixed per doc-mirror-files-are-cli-only); carton_utils doc(m) committed 4c00e54. Chain_Step_Close_Phase1 flipped to done {closed_at, commits} via set_properties; Carton_Power_Chain current_order=2. NEXT: Step 2 Small_Fixes builder batch (render_through_template properties-merge, chroma cache NameError server_fastmcp.py:2675, edit-carton-kv skill dead-path correction).  tags:[Carton_Power_Chain, doc-mirror-commit]
