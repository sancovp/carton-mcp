# vision: abstraction-cypher (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-10T17:38:28  FINDING: PULLED sancovp/abstraction-cypher (private) to /tmp/abstraction-cypher; read 100% (DESIGN.md 129L + engine.py 704L + runner.py 273L + test_game_loop.py 305L). WHAT IT IS: Sh8peshift RPG — a GRAPH-NATIVE STATE-MACHINE EXECUTION ENGINE on plain neo4j properties+edges: StateMachine-HAS_STEP->TraversalStep{text=prompt, required_pattern=gate-regex, expected_diff} with NEXT_STEP{weight} transitions; IdentityState{turn_number,phase,call_stack}-CURRENT_STEP->step = the cursor is an EDGE; CALLS_SM = sub-state-machine calls with a JSON call-stack property (push/pop, engine.py:267-318,460-519); day/night turn loop; evolution = fitness-gated reap/survive/reproduce with property mutation (evaluate_evolution :552-695). DIRECT BEARING: this is a richer prior-art schema for Carton_Power_Chain step 3 Retrieval_As_Data — our planned Retrieval_Protocol/Step{order,cypher,when} is a subset; Sh8peshift adds required_pattern gating, weighted NEXT_STEP edges, cursor-as-edge, and a call stack. CAVEATS (IS vs DESIGN): engine.py imports query_database/is_traversal_locked/progress_traversal from a MISSING sibling ../neo4j_cypher_mcp/server (engine.py:260-265) — repo does NOT run standalone; nothing in-repo bootstraps the default sh8_lifecycle_sm steps; AgentLLMRunner is a mock returning canned queries (:31-53); TraversalState lock is a global singleton (single-player); DESIGN's night weight-reinforcement (+0.1/-0.2 on NEXT_STEP) is NOT implemented in engine.py (calibration only records random accuracy :321-340) — vision, not impl.  tags:[abstraction-cypher, sh8peshift, state-machine, neo4j-properties]
- [v2]  2026-06-10T17:39:57  FINDING: RE-PULLED abstraction-cypher (full codebase, commit b184f09): now TWO packages — abstraction/ (the Sh8peshift files already read) + neo4j_cypher_mcp/ (server.py 1441L READ 100% + 7 test files + start.sh + pyproject). server.py = an MCP server ('neo4j-cypher') that is THE missing half: (1) query_database = free-Cypher agent surface that BLOCKS write mutations targeting :Wiki (validate_cypher_query :247-265 — explicitly designed to sit BESIDE CartON and protect it); (2) QUERY-PATTERN GATING: while a TraversalState lock is active, write queries MUST regex-match the current step's required_pattern (or a transition's pattern at decision points) or they are REFUSED with the step instructions — matching queries auto-progress the cursor (:479-603); (3) trigger_traversal: any query result containing a node with that property AUTO-STARTS the state machine lock (scan_and_trigger_traversal :380-449) — touching a task node starts a guided, write-gated checklist = the graph programs the agent, literally; (4) create_traversal_flow / create_weighted_transition / adjust_transition_weight = runtime authoring of flows as graph data; (5) crud_surrogate simulate/calibrate = softmax path simulation over NEXT_STEP weights (selection_pressure temperature, mutation_rate exploration) + calibration = expected-vs-actual diff overlap accuracy that ADJUSTS weights along the simulated path (:899-1245) = a predict->track->compare->learn loop. CORRECTION of my 17:38 entry: weight reinforcement (+0.1/-0.2) IS implemented — in server.py adjust_transition_weight :846-897 and wired into crud_surrogate calibrate :1209-1217; it is absent only from engine.py's night phase. NEW DEFECT FOUND: server.py's RPG wrapper tools sys.path at sibling '../sh8peshift' (:1297,:1330,:1356) but the dir is named 'abstraction' — the 3 MCP RPG wrappers are import-broken as shipped; engine->server direction ('../neo4j_cypher_mcp') resolves fine. BEARING: this is a designed, fuller prior-art for Carton_Power_Chain step 3 (Retrieval_As_Data) — protocols as TraversalStep chains + pattern-gated execution + learned weights; step 3 should be designed AS a fold-in/port of this rather than a from-scratch runner. Tests not yet read (7 files ~1170L).  tags:[abstraction-cypher, neo4j-cypher-mcp, query-pattern-gating, surrogate-model]
- [v3]  2026-06-10T17:48:41  DECISION: INTEGRATION SYNTHESIS FROM THE JOURNAL (Isaac: 'find out how we should integrate it from reading the journal first' — abstraction-cypher was made as a TOY 2026-06-10 to SHOW the carton direction). The toy is a built instance of THREE prior journal designs: (1) Treeshell_Flight_Over_Carton 2026-06-09 (Isaac's unified design): 'CartON traversal substrate with gates (cannot leave until you define X)... PROGRAM A FLIGHT OVER CARTON... provable self-governance via OBSERVABLE STATE MACHINES over CartON graph-chains-with-KV-programs; NO symbolic logic — just observability + state machines' + PROOF OBLIGATION 'ONE flight programmed over CartON that GATES on a node, walks the graph filling schemas, assembles a response E2E' — the toy IS that proof obligation: TraversalState lock = cannot-leave-until-defined (writes refused unless matching required_pattern), create_traversal_flow = programming the flight AS GRAPH DATA, CURRENT_STEP edge = the observable cursor, trigger_traversal = touching a node starts the flow. And it DISSOLVES the heavy integration the 06-09 entry feared (pack starship/waypoint/PD/omnisanc into treeshell): the substrate is just the graph + a gating query surface. (2) Composite_Skillchain_Design 2026-06-04 (parked): Isaac asked verbatim for 'package up a state machine VALIDATOR' + 'a PreToolUse HOOK for the skillchain so it has a state machine' + the open seam 'the chain is NOT REIFIED (implicit in cursor+CoRs+transition table)' — the toy reifies the chain as TraversalStep/NEXT_STEP graph objects and implements the validator as query-pattern gating; the doc-mirror transition guard and the toy's gate are the SAME validator at two layers (tool layer / graph-write layer). (3) Publishing_Track_Continuation 2026-06-09: PUB-1..6 done; next = deployments + Publishing-V2 content-gen; the publish_unit pipeline (archive->rsync->README autogen->tag-push->CI->marketplace sha) is a multi-step stateful repeatable process living in bash+skills+session memory = exactly what should become a GATED FLOW over Publishing_Unit property-nodes (can't advance until CI-green query matches; statuses flip only through the gated path). INTEGRATION SHAPE (proposal, not yet Isaac-ratified): fold in neo4j_cypher_mcp as its own MCP beside carton (it already :Wiki-protects; classify-before-build = substrate engine); Carton_Power_Chain step 3 = stand it up + author retrieval/DMN protocols as TraversalStep flows; step 4 gains publish-as-gated-flow; Sh8peshift identity/evolution layer maps to personas = deferred tier. KEY GAP for our use: the toy gates CYPHER text, but our agents act through carton tools + bash — either route work through query_database, or generalize the gate to tool-calls (= the 06-04 PreToolUse skillchain hook). The 06-09 SOPHIA progressive-disclosure-traversal-filling-schemas = the toy's step.text prompts + expected_diff.  tags:[abstraction-cypher, treeshell-flight-over-carton, skillchain, publishing]
- [v4]  2026-06-10T17:54:26  DECISION: ISAAC RATIFIED + EXTENDED the integration (2026-06-10): (1) EQUIVALENCE (Isaac verbatim sense): 'a state machine is really the same as a skill... these composed extended state machines are skillchains. Its equivalent... a SM could literally say pull this concept which has a path on it and go read whats in the path — thats a skill, literally a stupid version of it.' This matches the 06-04 finding exactly (links=skills; StateConfig(goal,tools,prompt_suffix)=a skill; composite = orchestrator-wraps-inner turtles-all-the-way-up, heaven_base/state_machine.py:12-13 re-grounded today). CANON: skill=state, skillchain=state machine, composite skillchain=orchestrator-wrapping-inner. (2) SKILLS MIRRORED VIA PROJECTION: put all skills as concepts projected out via substrate_projector (already proven capability) — even if initially just for PUBLISHING (each skill package shipped with CI/CD as a plugin in the marketplace); with cd37c66 props-merge, skill frontmatter=properties + body=render. (3) ARCHITECTURE DECISION (Isaac): abstraction-cypher does NOT become its own thing next to carton — 'we wanna marry them so this is part of carton entirely. Because it doesnt do anything that is anti-cartonly.' CONSEQUENCE: flow nodes (TraversalStep-like) become ordinary carton concepts in the same graph (journaled/linked/RAG-able/property-tracked); the GATE moves from cypher-text regex to CARTON'S OWN TOOL SURFACE (set_properties/add_concept/query refused unless the active step's condition is met) — which RESOLVES the key gap flagged at 17:48 (agents act through carton tools, not raw cypher); the toy's carton-foreign parts (own driver, own labels, :Wiki write-block) dissolve. (4) Heaven KeywordBasedStateMachine + compoctopus StateMachine(GenericS)/make_compiler_sm = the already-built agent-side SMs (minimax agents can carry them); the carton flow layer is the GRAPH-side of the same machine. STEP 3 RE-SCOPED: Retrieval_As_Data = build the carton-native flow layer (flow-as-concepts + tool-surface gating + cursor-as-edge + weighted transitions), porting abstraction-cypher's mechanics INTO carton-mcp rather than running its MCP beside it.  tags:[abstraction-cypher, skill-sm-equivalence, marriage-into-carton, compoctopus]
