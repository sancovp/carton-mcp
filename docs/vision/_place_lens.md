# vision: Gnosys_Game (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-21T01:48:35  DECISION: LANDSCAPE + ZONE PRIMITIVE (Isaac 2026-06-21). The FOUR LENSES named as FOUR AUTONOMOUS ROLES, and PLACE finally placed: (1) STEWARDS = Domain lens = autonomous agents webbing/populating carton (facts). (2) STARSHIP = Code-Object lens = autonomous agents developing codebases (the pilots/evolvers). (3) AGENTS = Agent lens = agents running workflows WITHIN the game. (4) GNOSYS = Place lens = the system accessing + orchestrating it LIKE A GAME, autonomously = places = treeshell families + zones. (This REFINES my earlier loose 'Place = the agent world-context' -> Place = GNOSYS, the game-orchestrator over treeshell zones.) === THE ZONE PRIMITIVE (Isaac, the clarification): a treeshell ZONE = an ARBITRARY FAMILY OF NODES joined together for NO ontological reason -- ONLY to constitute a GAME WORLD -- which then USUALLY has some kind of GIGANTIC STATE MACHINE (xp / levels / progression). So a zone is ORTHOGONAL to the meaning-graph: is_a/part_of = what a node IS; a zone = a GAME-GROUPING laid over it. A node can sit in any zone(s) without touching its meaning. (This is distinct from the 4 LENSES, which ARE is_a edges; a zone is a non-ontological game-grouping.) === TIES TO IN-FLIGHT WORK: the gigantic game SM == the sm_gate/Sm_Chain per-concept SM stack (the gearing-ladder==runtime-stack resolved this session) + the reward/kardashev score_compiler (the ONE live flow = xp). #2 (dragonbones ECs that drive state machines) = how you AUTHOR those game SMs. === IS vs VISION: IS = treeshell zones exist (missions/landing/crystal_forest=HOME/planetside); the per-concept SM stack (sm_gate) built; score_compiler (xp/kardashev) = the one live flow. VISION = GNOSYS autonomously orchestrating the 4 roles AS A GAME over the zones + the gigantic progression SM. === SPEC IMPACT: the agent spec grows a 4th role (GNOSYS/Place = game-orchestrator over zones); ZONE becomes its own primitive (arbitrary-node-family + a game SM, orthogonal to meaning). (Prescan: Skill_Understand_Treeshell_Starsystem_Gap, Pattern_Dchain_Is_Forced_Implication_Cascade.)  tags:[Gnosys_Game, Treeshell_Zones, Place_Lens, Game_State_Machine]
