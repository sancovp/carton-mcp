# vision: webbing-agent (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-07-04T03:05:54  FINDING: FINDING: Webbing-agent coherence daemon (task #51) built by cloning Sophia's architecture 1:1 (Isaac's direct correction: 'we just made the sophia coherer agents that work just fine. why not use that pattern? doesnt that have a daemon?'). Two new files at knowledge/carton-mcp/ root (package maps repo-root to carton_mcp, confirmed via pip install --no-deps + import carton_mcp.webbing_agent -- the build brief's proposed carton_mcp/ subpath was wrong for this repo, corrected by the build agent and flagged, not silently done): webbing_agent.py (SDNAC primitive + pure _next_batch/_is_underdeveloped/_build_batch_goal eligibility detection, mirrors docmirror-cohere) + webbing_agent_worker.py (thin PID-locked driver, mirrors sophia_worker.py, ships DRY by default via a live.flag gate). One-line SYSTEM_SOURCES addition in observation_worker_daemon.py adds 'webbing_agent' so its own self-created concepts are excluded from re-atomization -- the Provenance_Tracking_Main_Vs_Daemon_Origin recursion guard, reusing the EXISTING CHAT_SOURCES/SYSTEM_SOURCES substrate rather than building new provenance machinery. Dev-flow skill (edit-the-webbing-agent) + repo rule (editing-webbing-agent-uses-the-dev-flow.md) written per every-build-ends-in-a-development-flow-skill. I independently verified, not just trusted the report: ran the 10 unit tests myself (green); ran my OWN query_wiki_graph queries against the live graph (not the agent's) confirming the real E2E test concept (Webbing_Agent_E2E_Test_Concept_20260704) landed real instantiates/produces/has_part structure + webbed=true, its two new children carry source=webbing_agent, and a fresh eligibility check on all three returns still_eligible=False for all three (recursion guard + mark-processed both proven live, by me); confirmed via grep that _live() requires an absent live.flag/env var so it genuinely ships dry; confirmed git status shows nothing committed. OPEN, Isaac's call before flipping --live: the DEFAULT_SCORE_THRESHOLD=50/DEFAULT_MIN_REL_COUNT=2 eligibility thresholds are judgment calls, not derived from spec; one live test run showed the agent can over-interpret prose (invented plausible child concepts from a description that was itself about testing) -- worth watching a few more dry batches first. NEXT: await Isaac's go-ahead to flip live.flag, or task #52 (CI code-review automation, blocked on his branch/PR-model decision), or other direction.  tags:[webbing-agent]
- [v2]  2026-07-04T04:01:09  FINDING: FINDING: Post-compact-12 boot (2026-07-04). Rehydrated per DMN: conversation-0 = 5 entries read in full (2026-07-04T02:28:52 through 03:37:21, the post-compact-11 boot through the personal_domain fix, the enforcement-bypass audit, and the verify-rehydration-before-compact close-out). All four touched cohered notes read in full, consistent with the timeline, zero new contradictions. Cursor confirmed idle, pathway matches the journal (all remaining backlog gated on Isaac). Found the tree was NOT clean at boot -- the webbing-agent coherence daemon (task 51: webbing_agent.py, webbing_agent_worker.py, test_webbing_agent.py, the dev-flow skill/rule, four auto-generated vision-doc stubs, the SYSTEM_SOURCES addition in observation_worker_daemon.py, and this conversations own journal deltas) had been fully built and E2E-verified last session but never committed. This is not a new decision needing Isaacs go-ahead -- the open judgment call he flagged was specifically the live-flag flip and eligibility thresholds, not whether to persist already-verified dry-shipping code -- so committed it directly, per the standing discipline against leaving verified work uncommitted across a compact. Also hit the same recurring gap as be62089 and the post-compact-10 entry: the blanket .gitignore:32 .claude/ rule was silently excluding knowledge/carton-mcp/.claude/ (the new dev-flow skill/rule plus three pre-existing untracked rule files plus CLAUDE.md) -- force-added the whole directory per the established carry-forward pattern. Committed as 4565dc5, pushed, HEAD==origin/main, tree clean. NEXT: nothing newly unblocked -- everything remains gated on Isaac (task 58 enforcement-gap fix, SOMA mereo-C, task 52 CI review, task 47, task 57 query-cache, webbing-agent live-flag flip). Going idle.  tags:[carton, webbing-agent, git-durability]
