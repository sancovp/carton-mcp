# vision: Agent_Identity (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-23T04:18:13  FINDING: BUILT + VERIFIED the actual-identity model slice. sm_gate.py: _identity_node_name(handle) (TitleCase _-segments + ensure _Identity suffix; 'gnosys'->'Gnosys_Identity') + resolve_identity_entity(handle, run) (MERGE entity is_a Agent_Identity + HAS_COLLECTION->{X}_Collection if it exists; FAILS-OPEN). server_fastmcp.py _sm_actor(): resolves the raw carton_identity handle through resolve_identity_entity so the gate's lifecycle/Execution_State attaches to the real Agent_Identity entity (only reached when gate ON; FAILS-OPEN to the handle). VERIFIED LIVE: tests/test_identity_entity_resolution.py 4/4 (name mapping; resolve MERGEs entity+HAS_COLLECTION idempotent; the resolved entity is a usable gate actor sm_chain_visit-arms+get_active_step; real-world sanity gnosys->Gnosys_Identity + Gnosys_Collection exists) + server chokepoint probe (srv._sm_actor('zztest_srvwire_probe')->'Zztest_Srvwire_Probe_Identity'). NO REGRESSION: test_sm_core_live_wiring + test_sm_gate_e2e + test_sm_factory_live_e2e all still PASS. pip install --no-deps done; LIVE MCP needs reconnect_mcp carton (user-only) to pick it up but the gate is default-OFF so no live impact. This RESOLVES the latent 'gnosys has 0 :Wiki nodes' Core-gate finding: when the gate is enabled, _sm_actor now resolves+MERGEs Gnosys_Identity so _lock_into_sm_chain locks a real node. Equip_persona (3 impls) + heaven UNTOUCHED (resolution is lazy at gate-read).  tags:[Agent_Identity, Sm_Gate, Has_Collection, Built]
