# vision: Fence_Preservation_Guard (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-09T22:15:12  DECISION: NEW REQUIREMENT (Isaac): fence-PRESERVATION guard = the symmetric completion of fence-integrity. THE TRIO: (1) fence-OPACITY (linker can't corrupt a fence) DONE; (2) edit_carton_obj (surgical body edit) DONE; (3) fence-PRESERVATION (NEW) = an ordinary description/prose WRITE cannot accidentally delete a CartonObj. DANGER =  mode (which we just made actually work) + prose re-derivations: add_concept(concept,'new prose',replace) would wholesale-nuke n.d incl any CartonObj. MECHANISM: a PYTHON pre-step in the daemon write path (observation_worker_daemon batch, before SET n.d — Cypher CASE can't extract fences): if the concept's CURRENT n.d has CartonObj fences, any fence present in OLD n.d but ABSENT BY NAME from the incoming description is CARRIED FORWARD (re-attached verbatim). So replace preserves fences; append/prepend already safe; edit_carton_obj unaffected (fence present by name → edited body wins, no double-add). WHOLE-FENCE REMOVAL requires an EXPLICIT op (add edit_carton_obj op=remove_fence) — NEVER via ordinary prose write. NET: a CartonObj is a DURABLE SUB-STRUCTURE — only edit_carton_obj changes its body, only explicit remove deletes it, everything else leaves it byte-intact. Added as task #9 (STEP 4B), sequenced after Step-4 gate, before Step-5. Confirm-pending: whole-fence removal=explicit-op-only (the 'cannot accidentally remove' intent). Step-6 dev-flow coherence set must include this guard.  tags:[Fence_Preservation_Guard, Fence_Integrity_Trio, Durable_Substructure]
