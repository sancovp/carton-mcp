# vision: heaven_base (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-28T18:44:30  FINDING: FINDING: ROOT-CAUSE of task-54 fd-1 stdout leak FULLY TRACED + the task premise CORRECTED (it is NOT an fd-1 leak). REPRODUCED on the live installed packages: after CartOnUtils.query_wiki_graph, a bare print() lands on STDERR while sys.stdout.write / print(file=sys.stdout) / os.write(1) ALL still reach STDOUT, and os.fstat(1) is unchanged. So fd-1 is NEVER redirected. The corrected mechanism (proven by capturing builtins.print identity before/after): during the query, builtins.print is REPLACED with heaven_base._stderr_print (module heaven_base, qualname _stderr_print) which does kwargs.setdefault(file, sys.stderr) then calls the original print. SOURCE = base/heaven-framework/heaven_base/__init__.py:12-17, an UNCONDITIONAL module-top-level clobber: if os.environ.get(HEAVEN_ALLOW_STDOUT) != 1: builtins.print = _stderr_print. INTENT (stated in the file comment, IS): redirect print to stderr to prevent MCP stdio JSON-RPC protocol corruption; documented escape hatch HEAVEN_ALLOW_STDOUT=1. WHY it fires during the query not at carton import: carton imports heaven_base LAZILY inside _get_connection / _create_graph_connection (from heaven_base.tool_utils.neo4j_utils import ...); the first query triggers import heaven_base which runs the clobber. The earlier 2026-06-26 fix_sophia_progress.md diagnosis (an OS-level dup2 of fd-1 never restored) was WRONG about the mechanism (no dup2 occurs; os-level fd-1 is intact) though the symptom + the os.dup(1)/os.write guard workaround it produced are correct + still load-bearing. WHY THIS IS AN ARCHITECTURAL FORK, NOT ISOLATED HYGIENE (so NOT autonomously shipped): the redirect is RELIED UPON by every MCP stdio server in the system for protocol safety; a LIBRARY cannot know at import time whether its importer is an MCP server; isatty() does NOT discriminate (sophia_worker runs the CLIs via subprocess capture_output so their stdout is a PIPE = isatty False = looks identical to a piped MCP server). So the only correct fix is ENTRYPOINT-CONTROLLED opt-in/opt-out (MCP server entrypoints enable protection; plain CLIs do not), which is high-blast-radius across all MCP servers with a silent-protocol-corruption regression risk if mis-done. RECOMMENDATION (Isaac-call): (A) flip heaven_base to opt-IN: default keeps print, add enable_mcp_stdout_protection() that every MCP-server entrypoint (mcp.run sites) calls -- correct but must find ALL entrypoints or that server corrupts silently; (B) keep default-on, have the known stdout-needing CLIs set HEAVEN_ALLOW_STDOUT=1 (the documented hatch) -- low-risk, replaces the fragile per-CLI os.dup guards, but per-CLI not systemic; (C) leave as-is, document the hatch. DEFERRED to Isaac (architectural). The docmirror CLIs keep their working os.dup(1) guards meanwhile (no regression). NOT shipping a foundational-default change autonomously. No backticks.  tags:[heaven_base]
