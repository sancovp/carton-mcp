# vision: State_Machine (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-21T02:23:36  FINDING: PIECE 1 DONE + VERIFIED BY ME + COMMITTED (b200dd2). create_sm_chain added to carton-mcp/sm_gate.py (after skill_to_sm): idempotent generic SM factory -- concept -HAS_SM_CHAIN-> Sm_Chain -SM_CHAIN_RUNS{order}-> State_Machine -HAS_STEP-> Traversal_Step{required_pattern,text} + NEXT_STEP; uses the module T_*/R_* constants; abstracts the proven test_sm_core_e2e _program cypher; returns {concept, sm_chain, sms, steps, gated:len>1}. VERIFICATION (I ran it myself, not the agent's word): tests/test_sm_factory_e2e.py 3/3 PASS exit 0 -- (1) structure queried back correct, (2) sm_chain_visit ARMS require_next + locks at the gating step on the factory-built SM = it genuinely GATES (not just nodes), (3) idempotent (double-call same counts). Self-cleaning (Zztest_Factory_ prefix). pip install --no-deps carton-mcp done (function importable). I read create_sm_chain source myself (sm_gate.py:470) -- matches design. Note: NOT yet exposed as an MCP tool (library function only, per spec scope; add the MCP tool when dragonbones/treeshell need the agent-callable surface). NEXT = PIECE 2: dragonbones State_Machine EC + routing (compiler.py:554 seam) that CALLS create_sm_chain -- requires designing the SM EC SYNTAX (agent-facing contract; ground in parser.py for how steps/nested-claims express; propose a concrete example EC for Isaac's nod before wiring). THEN piece 3 (skill) + the treeshell-lang dragonbones-duplication fix (Isaac's order).  tags:[State_Machine, Sm_Factory, Verified]
- [v2]  2026-06-21T03:33:56  FINDING: PIECE 2A DONE + VERIFIED BY ME + COMMITTED (c423e37). create_sm_chain_live(concept_name, state_machines, sm_chain_name) added to carton-mcp/sm_gate.py: self-contained library entry (opens own KnowledgeGraphBuilder from env via the proven _conn/_mk_run pattern, calls create_sm_chain) = the function dragonbones imports per the nuclear rule (caller passes only the spec, never a connection). VERIFIED (I ran it): tests/test_sm_factory_live_e2e.py 2/2 PASS exit 0; importable from installed pkg. === NOW PIECE 2B (dragonbones SM EC, approach A) -- KEY SEMANTIC nailed: to actually GATE, a Sm_Chain needs >1 SM (stack-size rule: a lone SM is OFF/show-only). SO the SM EC declares the GATING steps, and the dragonbones handler builds the STANDARD 2-SM STACK: order-0 = auto show-SM ({gated}_Show, 1 step, no pattern) + order-1 = the gating-SM (the EC concept_name) with the declared steps -> create_sm_chain_live(concept=<gates target>, [show_sm, gating_sm]) = a real gate. (Design choice, state-what-is: auto-show-SM = natural show-then-gate; adjustable.) SM EC SYNTAX A: operator + <SM name> {is_a=State_Machine, gates=<Concept_X>} then one claim per step {step=<id>, required_pattern=<regex>, text=<...>, next=<id>}. Parser change = additively preserve per-claim KV groups (a claims field) so the handler reads step-fields as PROPERTIES (bypassing relationship-flatten). Dispatching 2B w/ full semantics; verify E2E (SM EC string -> parse -> compile -> carton has GATING Sm_Chain on the gated concept).  tags:[Verified, Sm_Factory]
- [v3]  2026-06-21T03:40:35  FINDING: PIECE 2B DONE + VERIFIED BY ME + COMMITTED (3d2ea11). === APPROACH A COMPLETE: dragonbones exposes carton retrieval-state-machines FIRST-CLASS. parser.py preserves per-claim KV groups (additive 'claims' field; existing ECs byte-identical -- I verified a normal EC parses unchanged). compiler.py: is_a State_Machine routing branch -> _create_state_machine builds the standard 2-SM gating stack (auto show-SM order-0 + declared gating-SM order-1) -> create_sm_chain_live (the nuclear-rule library factory). E2E (I ran tests/test_sm_ec_e2e.py): 5/5 PASS -- SM EC string -> parse -> claims -> compile -> carton 2-SM gating Sm_Chain -> sm_chain_visit ARMS the gate. So an agent's dragonbones EC now authors a WORKING carton retrieval-gate. THE #2 GOAL ('dragonbones has to expose the carton SM stuff first class') = DONE for approach A. === NEXT = B + TREESHELL-FIX (they CONVERGE): the structured 🐉🦴 NESTED SM annotation (treeshell's lang_arg_parser.parse_db_block already parses nested graphs) routed through a SHARED TYPED-ROUTER -> the SAME factories (create_sm_chain for SM, giint for giint, add_concept untyped). Today treeshell node_sync._db_graph_to_concepts flattens EVERYTHING to untyped raw_concept -> carton queue, BYPASSING the factories (the duplication). The fix = add a typed-router to treeshell's nested-graph dispatch: is_a State_Machine -> create_sm_chain_live; typed-giint -> giint factory; else -> existing untyped queue path. = B (SM via nested annotation) AND the treeshell-fix (typed annotations through factories) in ONE. Grounding now (reading lang_arg_parser parse_db_block + node_sync seam).  tags:[Verified, Milestone]
- [v4]  2026-06-21T03:59:32  FINDING: B + TREESHELL-FIX: CODE COMPLETE + INTEGRATED-PATH-VERIFIED BY ME + COMMITTED (65035bd). node_sync typed-router: a parsed 🐉🦴 graph is_a State_Machine -> create_sm_chain_live (the SHARED factory dragonbones also calls); non-SM graphs keep the untyped queue path (additive). So treeshell annotate-on-add now routes TYPED SM annotations through the SAME factory as dragonbones (the de-dup/unification) AND an SM is authorable via the nested 🐉🦴 form. VERIFIED: (1) tests/test_sm_typed_router.py ALL PASS (incl. non-SM regression to queue); (2) parser self-tests 32/0 untouched; (3) THE EXACT LIVE execution_engine hook sequence (execution_engine.py:346-356 = extract_db_blocks(validate=False) -> partition_db_graphs -> sync_db_graphs_to_carton) creates the gating Sm_Chain in the server's pyenv (I ran it: Zztest_Live_Concept got its 2-SM stack). === VERIFICATION LESSON (test-gap, for next time): the router test called extract_db_blocks + sync_db_graphs_to_carton DIRECTLY, BYPASSING partition_db_graphs (the live hook's gatekeeper between parse and sync). The live MCP E2E (verify-via-user-surface) CAUGHT that gap -- a unit/dispatch test that skips the integration gatekeeper can pass while the live path differs. The integrated-path test (extract->partition->sync) is the right level + now confirms the SM graph is COMPLETE (not stashed) -> routes correctly. === LIVE MCP E2E = DEPLOY-BLOCKED, NOT a code bug: the running sancrev MCP server holds STALE in-memory node_sync (a 06-20 duplicate process PID 98126; reconnect_mcp spawned a new PID 48956 but did NOT switch the client). Live SM annotation via the MCP created nothing while a plain untyped 🐉🦴 landed (proving inline-db fires, only the new router is stale). FIX = a CLEAN sancrev MCP server restart (via restart-sse-treeshell), then re-run the live SM 🐉🦴 -> gating Sm_Chain. DO NOT pkill (mcp-reconnect-is-user-only). Flagged as a deploy step (risky to force AFK mid-session since I use this MCP). Cleaned up 28 Zztest_ test nodes.  tags:[Treeshell, Verified, Deploy_Pending]
- [v5]  2026-06-23T04:30:32  FINDING: LIVE-MCP E2E VERIFIED (Isaac authorized brief gate-enable; reconnected carton). Drove the LITERAL mcp__carton__get_concept tool: with a synthetic active identity 'zztest_livee2e' + the gate enabled + a seeded 2-SM gating Core, get_concept('Zztest_Livee2e_Subject') returned the served body AND appended '⛓ REQUIRED NEXT: run query_wiki_graph(...) next'. Post-call neo4j proof: the lock attached to the RESOLVED ENTITY 'Zztest_Livee2e_Identity' (status=locked, equipped_sm_id=Zztest_Livee2e_Gate_Sm) and the entity was live-MERGE'd is_a Agent_Identity — proving the server _sm_actor resolution + _lock_into_sm_chain locking a REAL node works end-to-end through the real surface. Gate DISABLED immediately after (enable-flag removed = kill switch; used a synthetic actor so my own gnosys session was never locked), ALL synthetic nodes cleaned (0 residue), active-identity restored to gnosys, gate confirmed OFF. The actual-identity gate-actor slice is now verify-via-user-surface COMPLETE.  tags:[Live_Mcp_E2E, Sm_Gate, Verified]
