# vision: Resolve_Identity_Entity (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-23T06:26:41  DECISION: S2 DONE: sm_gate.resolve_identity_entity now records the two-layer Agent_Identity shape as graph PARTS (MEANING -> edges, per property-layer doctrine). Added 3 optional kwargs frame/rules/skillset (default None -> gate-resolve path unchanged, default-safe): HAS_FRAME -> <Base>_Frame (.d=system-prompt text), HAS_RULES -> <Base>_Rules (.d=rules text), HAS_SKILLSET -> Title_Cased skillset node, alongside the already-wired HAS_COLLECTION + the SM Execution_State. Idempotent (MERGE). NO CCC labels (plain part-names). Added R_HAS_FRAME/R_HAS_RULES/R_HAS_SKILLSET constants. VERIFIED live neo4j: test_identity_entity_resolution.py extended with case 5_two_layer_parts -> 5/5 PASS (one HAS_FRAME with text, one HAS_RULES with text, one HAS_SKILLSET, idempotent); no-regression test_sm_core_live_wiring 4/4 PASS. The VALUES are filled by S3 (equip_persona). Projecting the frame as a running system prompt = host/claude -p, out of scope.  tags:[Resolve_Identity_Entity, Has_Frame, Has_Rules, Has_Skillset, Sm_Gate]
- [v2]  2026-06-23T06:35:19  DECISION: S4 DONE: server_fastmcp._ensure_identity_collection_exists now performs the collection->entity transition. Restructured: inverted the exists-guard (create the Identity_Collection only when missing — byte-identical create logic, just re-indented) so the function ALWAYS falls through to a best-effort _sm_gate.resolve_identity_entity(agent_identity, _sm_run) for BOTH the just-created AND already-existing collection. That MERGE-ensures the Agent_Identity ENTITY (is_a Agent_Identity) + links HAS_COLLECTION to the collection (the reflection store). Uses the in-process _sm_run (not the _live variant). NOT gated on _sm_gate_on (the entity is the identity MODEL, independent of SM-gating). Idempotent + never blocks the observation. No frame/rules/skillset (observe has only identity+collection; S3 equip fills those). NEW-collection case: add_concept is async so HAS_COLLECTION links on the next observe (eventual); entity is created immediately. VERIFIED: /tmp/s4_probe harness (pre-created collection = the existing-collection transition target) -> S4_OK True (entity_is_a_AgentIdentity=1, HAS_COLLECTION=1, idempotent on re-run); server_fastmcp imports clean. Non-breaking: collection still is_a Identity_Collection, entity layers on top.  tags:[Ensure_Identity_Collection_Exists, Observe_From_Identity_Pov, Resolve_Identity_Entity, Server_Fastmcp]
