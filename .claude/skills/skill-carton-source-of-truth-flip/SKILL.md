---
name: skill-carton-source-of-truth-flip
description: "WHAT: the implementation pattern for making CartON the source of truth for TreeShell node definitions — embed the raw node dict as a JSON marker in the concept description during sync (node_sync.py), so TreeShell nodes persist to and load from the CartON graph. WHEN: when implementing or changing CartON as the source of truth for TreeShell nodes, embedding a node definition as JSON in a concept description, or wiring TreeShell node loading to read from CartON."
---

'''Implementation pattern for making CartON the source of truth for TreeShell nodes. Two files changed: node_sync.py embeds NODE_DEF JSON marker in concept description during sync '''Replayable skill pattern - understand=context only''' '''TreeShell navigation graph system''' '''Skill category classification''' '''Personal domain category enum''' '''TreeShell navigation graph system''' '''CartON knowledge graph integration layer''' '''Embed raw node dict as JSON in CartON description '''When implementing CartON as source of truth for TreeShell node definitions''' '''TreeShell nodes that persist to and load from CartON graph''' '''Base TreeShell library at /home/GOD/heaven-tree-repl/'''

---
## Skill Contents

- `reference.md` — detailed reference (read for full docs)
